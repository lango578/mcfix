# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> 关于日期的说明：1.2.0 ~ 1.6.4 是同一个开发周期内的迭代，作者当时没有逐条记日期，
> 所以早期条目统一按打包日归档，不代表"一天发了九个版本"。1.7.0 起按实际发布日记录。

---

## [1.13.2] — 暴力安全测试回合：5 个高危

一轮授权攻击性测试（真实 PHP 8.3 + Node 模拟 MC/RCON，8 轮，25 个问题）。
常规 Web 攻击面是干净的 —— SQLi / XSS / CSRF / 命令注入 / 路径穿越 / 上传 /
未认证 SSRF **全部 0 命中**，AI 那条路因为原始日志根本不进模型上下文，
提示注入在结构上不可能。这轮的问题集中在**异常路径**上。

报告里有句话说得准：**失败/异常路径倾向于给出一个"明确但可能错误"的结论，
而不是承认"我不确定"。** 五个高危里有三个是这句话的实例。

> 施工说明：报告给的行级补丁我**逐条对着源码核过**，有几处行号对不上，
> 还有一处（V19）补丁本身不够、照抄堵不住漏洞 —— 下面都标了。

### 🔴 V1 — 匿名者能把任意 MC 账号加入白名单

`whitelist_add` 标的是 `medium`，而 `Recipe::needsApproval()` 只对 `high` 生效。
于是匿名 POST 一单 `category=permission` 就能让服务端真的执行
`whitelist add <任意名字>`，报告实测响应里还是 `"awaiting_approval": false`。

白名单是服务器的**准入闸门**，而 `player_name` 只是表单里自填的一串字符 ——
系统没有任何办法证明填表的人就是那个账号。改成 `high`，走管理员批准队列。

（同类问题作者此前在客户端日志那条路上防过一次，但服务端 `category=permission`
这条路是另一个入口，没被那次修复覆盖。）

### 🔴 V2 — 一个请求头同时绕过后台 IP 白名单和登录锁定

`trusted_proxies` 默认含回环地址，而很多部署下 `REMOTE_ADDR` 恒为 `127.0.0.1`
（PHP 内置服务器、nginx 以 TCP 转 php-fpm、透明代理）。此时客户端自填的
`X-Forwarded-For` 被当作真实来源。

报告实测：配置 `ip_allow=['203.0.113.7']`，加一个 `XFF: 203.0.113.7` 就拿到
后台登录页；并且每次换一个 XFF 就等于换一个"身份"，**失败锁定永不触发**。

改了三处（只改一处不够）：

| 位置 | 改动 |
|---|---|
| `src/helpers.php` | `client_ip()` / `client_ip_source()` 的兜底默认值改成 `[]` |
| `config.example.php` | 默认值 + 把原来那段**误导性说明**改正（原文写"保持默认即可"，照做就会中招）|
| `public/controllers/install.php` | **安装向导写入的默认值**（不改的话新装站点照样中招）|

另加 `Config::diagnose()` 自检告警：有人在 `trusted_proxies` 里填了回环地址、
或当前判定取自转发头时，后台会明确提示。

### 🔴 V5 + V15 — 必须同一次改完（不然 V5 会重新触发 V15）

**V5**：`runLocal()` 把任务 JSON 用 `escapeshellarg()` 拼进命令行。PHP 在 Windows
上的 `escapeshellarg()` 走 POSIX 规则，**会把 JSON 里所有双引号删掉** → Agent
收到 `{ action : diag ... }` → 一律回"任务 JSON 解析失败"。

真正的危害不是"本地执行器不能用"：这个失败被降级成 `unknown`，而 `Verdict`
对 `unknown` 不分类 → 对外表现为"没有验证到服务器侧异常 → 转人工"。
**故障被伪装成了正常结论。**

改用 `proc_open()` 的 argv 数组形式（不经 shell），并新增 `phpBinaryPath()`
返回未转义的路径。`runSsh()` 同样的坑：改用 POSIX 单引号转义手写（目标 shell
是远端 Linux 的 sh，不是本机 cmd.exe）。

**V15**：熔断是**确定性死锁**，报告的四步证明我逐行核对过，成立：

1. `consecutiveFailures()` 没有时间窗，计数永不衰减；
2. 清零的前提是遇到一条"成功"记录；
3. 熔断分支在写记录**之前**就 `return`，被挡住的这次不写任何记录；
4. 那条成功记录永远不可能产生 → 永久锁死。

修法：计数加时间窗（`circuit_window_seconds`，默认 1 小时）、新增显式
「解除熔断」动作（后台服务器页有按钮）、熔断时记录事件并把 `已熔断` 关键词
放进错误文案（否则玩家侧看到"计划了但没做、也不说为什么"，和静默失败无法区分）。

> 报告建议用 `Config::getMeta()` + `settings_meta` 表存解除时间点 ——
> 这两个 API **在项目里不存在**。改用项目已有的 `Cache`（缓存文件），
> 与现有机制一致，也不用改 schema。

### 🔴 V18 — 匿名一条请求枚举服务器全部 mod/plugin 清单

`ServerMods::crossCheck()` 把"服务端有、客户端日志里没有"的文件名全放进
`client_missing`。而**日志章节名是提交者自己编的**（解析器只按 `stripos` 认
「Mods」这类词），所以随便写一节假 mod 列表，就能让服务端清单里**每一个**
文件都落进这个列表。

报告实测 **11/11 全量泄露**，名字精确到构建号（`LuckPerms-Bukkit-5.4.102.jar`
这种）—— 等于一份现成的 CVE 打点清单。三条泄露路径全堵：

| 路径 | 现在 |
|---|---|
| `cross.missing[]` | 改成只给 `missing_count`，不给名字 |
| `issues[].extra[]` | 只给数量 |
| `components[]` | `collectNeeds()` 不再把服务端清单当"玩家要装的 MOD"解析，只保留玩家日志/报错里**确实提到过**的名字 |

> 报告特别澄清过：这与那个 `available`/`possible` 键名笔误**无关**，
> 是两件独立的事 —— 所以没有去改键名充数。

### 🔴 V19 — 13/15 个分类误报"MC 服务端进程不存在/critical"

`case 'process'` 只要 `fail` 就无条件判 `process_missing/critical` + 建议
`restart_server`，**完全不看同一批检查里 `mc_status` 已经 pass**。于是同一份
响应里既有"服务器在线：3/100 人"，又有"进程不存在"，还引导管理员去**重启一台
健康服务器**。

面板托管、容器内运行、MC 在另一台机器、`local` 执行器而本机不直接跑 MC ——
这些情况进程表里都看不到，但服务好好的。

**★ 报告给的行级补丁在这里不够用。** 它让 `case 'process'` 调用
`evidenceServerAlive()`，但那个函数当时**只看 `process_info` 和 `process`，
不看 `mc_status`** —— 而报告自己举的场景恰恰是"只有 mc_status pass"，
此时反证返回空串，补丁形同虚设。

所以做了两件事：

1. `evidenceServerAlive()` 采信 **MC 协议握手成功**。这比进程表读数更硬 ——
   能完成协议握手并报出在线人数，说明进程活着、监听正常、还在处理请求。
   （仍然**不**采信单纯的端口探测：端口通但握手失败是模糊场景，那种情况
   宁可判离线让人来看，这条原设计我保留了。）
2. 加通用不变式 `guardAgainstSelfContradiction()`：任何断言"服务端不存在/没在跑"
   的结论（`process_missing` / `port_not_listening` / `server_offline`），
   只要同批检查里有"服务端活着"的证据，就统一降级为待确认并**清掉修复建议**。
   把它做成结构性不变式，而不是在每个 case 里各写一遍 —— 那种写法已经漏过一次
   （`mc_status` 有反证、`process` 没有）。

已确认这个不变式**不会误伤**真正的故障：`server_offline` 只在 `mc_status` fail
时产生，那时反证必然为空。

### 🟠 中危：三条同源问题

报告对这批的分级很克制，我按它的原话标注：**都不是现成的洞，是"迟早出事"的那类**。

**V6 — 一道静默失效的防线 + plugins 被分发给客户端**

`api.php` 里写着 `if (!empty($cross['available']))`，但 `cross_check` **从来没有**这个键（它的键是 `possible`；`available` 是隔壁 `server_mods` 的）。条件恒假 → 那段"只认服务端清单里的名字"的限制从未生效过，一直退回玩家自己编的名字。

报告说得很清楚：**因为同一段里第 241 行本来就是精确匹配（不是子串），所以这不是现成的洞**。它是一道失效的防线。改法两处：把键名改对，并给 `crossCheck()` 补上这个键，别再让调用方靠猜。

同时 `agent/mcfix-agent.php` 的取文件逻辑把 `plugins/` 一起扫了。plugins 是纯服务端组件（反作弊、权限、经济插件），客户端永远不需要 —— 列出来等于告诉玩家这台服装了什么，付费插件还能被取回去再分发。收窄成只扫 `mods/`（该函数唯一的调用方 `pull_mod`，它的错误提示本来就写着"放到服务端 mods 目录"）。

**V7 — 事件表没脱敏，而日志表脱敏了**

同一个项目两套标准：`app_log()` 过 `redact_secrets()`，`record_event()` 没过。而 `helpers.php:508` 那段注释明明写着「这些字符串会被写进 events 表、storage/logs 和 CLI 输出，而『操作日志』是管理员的常规界面 —— **密钥必须在这里就断掉**」。

可达链路是有的：`PanelAdapter` 会把上游响应体截 200 字拼进错误信息，而面板在报错时回显凭据是常见做法（项目还专门支持 `custom` 适配器对接自建面板），错误串再经 `record_event` 落库 → 工单时间线 → 持有链接的玩家。

三处都改了：`record_event` 两个字段都过脱敏、`PanelAdapter` 在源头就脱敏、后台"测试面板 API"的审计记录只留能力清单（不再把日志样本整包存进审计表）。

**V8 — 同一份数据两条路两种待遇**

`problem.detail` 没走 `redact_paths()`，而紧邻的 `playerChecks()` 走了。报告诚实标注：**没能构造出确定含绝对路径的消息**（带路径的多为 `unknown`，而 Verdict 不分类 unknown），所以这是防御缺口、不是已确认泄露。顺手也把 `fix.output` 一起补上，成本极低。

### 🟡 低危 / 加固：报告说"当前不可达"，我照样补了

这几条报告都明确写了「当前不可达」，理由是调用点恰好都安全。我认为**"不可达"依赖的是调用点的写法，不是那行代码的正确性**，所以还是补上了。

| 项 | 问题 | 处理 |
|---|---|---|
| **V9** | Agent 的 `log` 动作不校验租约（紧邻的 `report` 校验得很严） | 补租约校验 + `extra` 体积上限（控制层 + 存储层各一道）|
| **V10** | `Task::report` 用 `!==` 比较租约，是短路比较 | 改用 `hash_equals`（常数时间）。空串=特权这个危险默认当前不可达，但注释里标明了 |
| **V11** | 通知渠道的密钥字段直接回显进 HTML | 改成 `type=password` + "已配置，留空表示不改"（对齐 settings.php 既有做法）|
| **V12** | 后台接入页在 **GET** 渲染路径里写配置 | 去掉写操作，改用派生值算链接；落盘只发生在管理员显式点「重新生成」时 |
| **V13** | `quoteIdentifier` 是"删掉引号"而不是转义 | 加白名单 + 改成双写。报告说 46 处调用点全是字面量 → 不可达 |
| **V14** | `LIKE` 通配符没转义（**不是注入**，是语义） | 加 `ESCAPE '\'`；搜 `%` 不再等于不筛选 |

**V11 有个坑值得单说**：渠道配置是**整包替换**（`$items['notify']['channels'][$key] = $config`），不是合并。所以一旦密钥字段改成"不回显"，就必须同时保证"留空 = 沿用旧值" —— 否则管理员只是改了个超时时间，密钥就被静默删掉了，而且界面上什么提示都没有，渠道从此不再发通知。三态都处理了：填新值用新的、留空沿用旧的、勾「清除」才真删。

**V12 同理**：`ensureSecret()` 会 `rotate()` 落盘，而它在一个 GET 渲染里被调用。GET 应当只读，且会被浏览器预取／爬虫／链接预览自动触发。去掉之后 `ensureSecret()` 已无调用方，`rotate()` 只剩管理员显式点「重新生成」那一条路。

### 🟡 非安全 bug：三个会让排查困惑的问题

| 项 | 现象 | 根因 |
|---|---|---|
| **F1** | 「已处理」计数**恒为 0** | `$whereSql` 在勾了"只看未处理"时已含 `resolved = 0`，再拼 `AND resolved = 1` → 恒假。而且语义上它也不该跟着筛选走 |
| **F2** | 三个筛选表单提交后**404** | 表单里硬编码了 `<input name="r" value="admin.client">` 之类，而 `?r=admin*` 正是 index.php 明确要 404 的路由（不暴露后台位置）。表单本身已有正确的 `action`，那个字段只会把它覆盖掉 |
| **F3** | `?r=api.agent.report` **静默失效** | `public/index.php` 只取 `$parts[1]`，于是 `report` 被丢掉、`$action` 只剩 `agent`，剥前缀后变成空 → 落到默认动作 `poll`。而 `poll` 恰好也是默认值，所以"看起来能用"，其余动作全废 —— 可 `api.php` 的注释写的就是 `?r=api.<action>` |

**F3 我做了路由模拟才敢改**（10 个用例全过）：`?r=api.agent.report` 现在正确进入 `report`，而 `?r=api.submit`、`?r=api&action=submit`、`?r=agent.report`、`?r=mod.download`、裸 `?r=agent` 全部行为不变。

### 测试

新增 60+ 条断言，带 ★★ 的都是"报告说的那个洞现在被堵住了"的直接判据。几条关键的：

- `Recipe::needsApproval('whitelist_add') === true`（行为断言，不是只看 risk 字符串）
- 未配置可信代理时 `client_ip()` 忽略 XFF ＋ **负向对照**：显式配置后仍然采信
- 低风险动作（`save_world`）**仍然自动执行**（别把整个自动化一起锁掉）
- `evidenceServerAlive` 必须读 `mc_status`（专门盯报告原补丁漏掉的那点）
- V20 的四个假日志行**不再**命中，九个真实踢出/封禁措辞**仍然**命中
- V3 用真实占位符名单模拟：`change-me`、`secret`、`CHANGE_ME_AGENT_TOKEN` 都不再被当成有效 seed

写测试时自己踩了两个坑，都记在这儿免得重犯：
1. `quoteIdentifier` 这名字在 `Db.php` 里出现 **14 次**（13 调用 + 1 定义），断言只搜名字会匹配到调用点、测了个寂寞 —— 改成锚在函数定义上。
2. 断言"旧写法已消失"时，**自己的说明注释里就写着那行旧代码**，导致断言必然失败 —— 这类断言不能直接搜子串。

**仍未能跑 `tests/run.php`**：本机没有 PHP（报告作者的靶场环境已经不在了）。上述结论来自源码逐条核对 + Node 等效模拟（V19 逻辑 11 项、V20 正则 13 项、V3 派生 9 项、F3 路由 10 项、各轮结构检查）+ `check-alt-syntax.js`（73 文件 0 问题）、`check-close-tag.js`、全部改动文件的括号配平。**真实 PHP 环境跑一遍还得靠你。**

### 🟡 玩家反馈页的"上传日志"教程写死了启动器和系统

原来写的是：

> 按 <kbd>Win</kbd> + <kbd>R</kbd>，粘贴 `%appdata%\.minecraft\crash-reports` 回车

这是**按 Windows + 官方启动器**写的。用 HMCL、PCL2、整合包启动器的玩家对不上；macOS / Linux 玩家直接卡住；`%appdata%` 还是个默认隐藏的目录。而玩家本来就知道自己的游戏文件在哪 —— 该教的是"从启动器打开游戏目录"这个通用办法，不是一条只对某一种环境成立的路径。

改成三层，从通用到具体：

1. **通用讲法**：选中最上面报错的实例 → 点启动器里「打开游戏目录 / 打开文件夹」→ 找 `crash-reports`（崩溃过才有）或 `logs/latest.log`
2. **按启动器对一下**（折叠着，找不到时才展开）：HMCL / PCL2 / 官方启动器各一行
3. **兜底**：翻不到就用「方式二」把报错文字粘过来，不用较劲

第 2 层标了 **❓ 待查证** —— 各启动器按钮叫法会随版本改，我没法逐个核实。这是项目既有的规矩（`deploy/主机商接入索引.md` 里写着）：**写错的教程比没有教程更糟**，所以宁可标着也不猜。同时那一段也给了"没有这些字样就找『打开文件夹/打开目录』类按钮"的通用出口，不让人卡住。

### 🟡 工单终态：驳回后一直显示"待处理"，标记已解决也不关闭

用户反馈：驳回之后工单状态还显示"待处理"；标记已解决之后工单没关闭。

先说清楚**不是显示层的锅**：`status_label('rejected')` 一直返回「已驳回」，列表页、详情页、玩家页也都是按真实 `status` 渲染的。真正的毛病在**落库的状态机少了两个动作**，以及由此牵出的一个隐私问题。

原本的设计是：

| 动作 | 写什么 | 问题 |
|---|---|---|
| 驳回 | `status = 'rejected'`，**不写 `closed_at`** | 不算"已结束"，归档任务跳过它 |
| 标记已解决 | `status = 'resolved'` | 要等 cron 每天收敛成 `closed`；cron 没配就一直是 `resolved` |

而 `purgeExpiredLogs()` 只清 `status = 'closed'` 的工单（它的注释理由是"resolved 可能还要重新分析"）。**驳回的工单永远进不了 `closed`，日志原文就永远清不掉** —— 而崩溃报告里含玩家的 Windows 用户名、显卡型号、游戏路径，玩家页面上却明写着"工单归档满 N 天后会自动清掉原文"。这不只是状态显示不对，是隐私承诺没兑现。

改法：

- **「标记已解决」直接落到 `closed`**，同时写 `closed_at`。不再留一个既非进行中、又不会被归档的悬空状态。
- **驳回也写 `closed_at`**，成为终态。`rejected` 和 `closed` 的区别只是"为什么结束"。
- **归档查询认三种终态**（`closed` / `resolved` / `rejected`），`bin/mcfix.php` 与后台「立即归档」两处的兜底收敛也一并覆盖 —— 兜底主要管升级前的老数据。
- 复验成功的两条返回路径原来回报 `'status' => 'resolved'`，现在与落库一致回报 `closed`。

**顺带发现一个我自己差点造成的回归**：日报的「自动修复成功」是按 `status = 'resolved'` 数的，状态名一改，这一行会永远显示 0，日报静默谎报"一条都没修好"。已改成按 `auto_fixed = 1` 数 —— 它本来就是这件事的真凭据（系统下发修复并复验通过才置 1，管理员手动标记明确置 0），语义更准，也不会再随状态名漂移。

测试里有一条原有的负向对照用的是 `resolved` 当"未归档"，和上面的语义已经冲突；改成用 `manual`（真正进行中）当负向对照，并补了两条新断言：`resolved`、`rejected` 满 30 天后原文**要**被清掉。

### 🟡 诊断里那句「（可考虑用面板控制台通道）」原来只是一句空话

管理员在诊断结果里会看到：

> 未开启 RCON，无法用控制台指令验证（可考虑用面板控制台通道）

查下来这句提示在那个位置**是误导的**，而且它指的通道当时也没接上。

**先说没接上**：`tps` / `player_online` / `whitelist_player` / `ban_player` / `whitelist` 这五项检查原来写死 `Rcon::fromServer($server)`，没开 RCON 就直接跳过。而面板控制台的 `sendCommand()` **只用在修复路径上**，检查根本没接 —— 所以"可考虑"其实没得考虑。

**再说为什么不能直接接上**（这才是关键）：这五项检查要**解析指令回显**才能得出结论，而面板通道和 RCON 在这一点上**不等价**：

| 面板 | 指令回显 | 能用来做这五项检查吗 |
|---|---|---|
| 翼龙 Pterodactyl | ❌ 只投递（`已投递到控制台`） | ❌ |
| Multicraft | ❌ 只投递（`已下发指令`） | ❌ |
| MCSManager | ❌ 面板接口不回显 | ❌ |
| 宝塔 | ❌ 没有控制台接口 | ❌ |
| 自定义 HTTP 接口 | ✅ 但**要配 `output_path`** | ✅ |

如果闭着眼把它们接到 `sendCommand()`，`rconTps()` 会拿 `"已投递到翼龙控制台（如需回显请用 readLog 或开启 RCON）"` 去跑 `preg_match('/(\d+\.\d+)/')`。匹配不到就是一句假的 `warn`；万一提示里有个数字，就会把它当成 TPS 报出来 —— **这比"跳过"更糟，因为它看起来有结论。**

改法：

- 新增 `PanelAdapter::commandEcho()`，**基类默认返回 `false`**（安全默认：谁都不许拿面板做需要解析输出的检查），只有确实能返回可解析回显的适配器才覆盖它。目前只有 `CustomPanel`，且**必须填了 `output_path`** 才返回 true —— 那句配置是管理员对着自己的接口写的明确声明"回显就在这个字段里"，比任何猜测都可靠。
- 新增 `CommandChannel`（+ `RconChannel` / `PanelChannel` 两个后端），把"发指令、拿回显"抽出来。五项检查改成接收 channel，解析逻辑一行没动。
- 选路规则：有 RCON 用 RCON；没有 RCON 但面板能回显，才退回面板；都不行才报"跳过"。
- `PanelChannel` 刻意把**空回显当成失败**：宁可明确报"读不到回显"，也不让下游拿着空串跑正则得出"白名单是空的"这类看起来有结论、其实是猜的判断。
- 那句提示语也跟着改了：能用的面板直接说清在用面板通道；其余情况给出真正可行的两条路（开 `enable-rcon`，或用自定义接口并填 `output_path`）。

新增三个文件都是**一个类一个文件** —— 项目的自动加载是按类名找文件的，塞在一起会直接 `class not found`（我第一版就是这么写的，验证时发现的）。

文档：`deploy/面板服接入指南.md` 的「坑 4」原本就写着"面板控制台没有回显"，现在补上了那个例外（自定义接口 + `output_path`）以及怎么排查。

### 发布前复核又挖出四处（其中两处是这一轮自己留下的）

**公开链接点「重新生成」不再作废旧链接。** 接入页是 GET，不能写配置（V12），
于是不再调 `Share::ensureSecret()`；但链接因此退回用**全局** `hmac_secret` 签名，
而全局密钥永远是 `Token` 的验签候选 —— 生成多少把新 `share_secret` 都换不掉它，
旧链接会一直有效到 30 天过期，页面上却写着"旧链接立刻失效"。
改成用**这台服务器专属的派生密钥**（全局密钥 + server id）签名，并且只在这台服
**没有**独立密钥时才把它算作验签候选：`rotate()` 一落盘，旧链接立刻验不过。

> 过渡口子要说清楚：**升级之前**发出去的链接是用全局密钥签的，全局密钥永远是
> 候选，所以那批链接只能等它自己过期（最长 30 天）。升级后页面上显示、以及之后
> 新生成的链接都是派生密钥签的，点「重新生成」立刻失效。

**反馈页的 session 从来就没开起来。** `$_SESSION['share_locked_server']`（专属
链接锁定服务器）与 `$_SESSION['my_email']`（回填邮箱）都写在/读在反馈页上，
而玩家侧唯一的 `session_start()` 在提交接口里。PHP 不会因为给 `$_SESSION` 赋值
就自动开会话 —— 那两个值不落盘、也不报错，提交时读到的一直是空串，防线看着在、
其实没接上。反馈页现在显式 `mcfix_start_session('mcfix_sid', …)`，与提交侧同名同作用域。

**`LIKE … ESCAPE '\'` 在 MySQL 下是语法错误。** PHP 双引号串里的 `ESCAPE '\\'`
落到 SQL 就是 `ESCAPE '\'`：SQLite 当成单字符转义符（正常），MySQL 认不出这半个
字面量（1064）→ 后台事件页 / 工单搜索直接 500。改用 `!` 作转义符，两个引擎都认。

**`consecutiveFailures($id)` 会因 int 数组键抛 TypeError。** 纯数字的 server id
在 PHP 里是 int 键，而 `admin/servers.php` 开着 `strict_types`，int 传给 string
形参直接 500。改成显式 `(string)`。

> 另记一条**行为变化**（不是 bug）：日志留存现在覆盖三种终态，所以升级后第一次
> `cron` 就会清掉那些"早就过了留存期、却因为只认 closed 而一直没清"的日志原文，
> 并把 `rejected` 收敛成 `closed`。这是有意的隐私收敛，但它**部署即生效** ——
> 升级前先备份数据库，并确认 `feedback.log_retention_days` 的取值。

---

## [1.13.1] — 反馈页在向玩家公开服务端连接地址

玩家反馈页把每台服务器的 `host:port` 直接印出来了。服务器状态卡片一行、
「出问题的服务器」下拉框一行，打开页面就看得见。

### 这为什么是漏洞而不是显示问题

地址是 MC 服务端的**可直连入口**。反馈链接的用途就是发到群里让玩家点，
所以"谁能看到这个页面"实际上不可控 —— 拿到 `host:port` 之后扫端口、打 DDoS、
绕过白名单尝试直连，都从这一步开始。而地址对玩家没有任何用处：他们要知道的是
"我玩的是哪台服"，不是"这台服连到哪个 IP"。

顺带查出来同一个数据还从另外三个地方出去：

| 位置 | 漏了什么 | 现在 |
|---|---|---|
| `?r=api&action=bootstrap` | 匿名可达，JSON 里带 `host` / `port` | 只回 `id` / `code` / `name` / `label` |
| `?r=api&action=ping` | 失败时 `message` 是 `stream_socket_client()` 的原文，里面带着连接目标 | 去掉 `message`（前端本来只读 `online` / `players` / `max`） |
| `feedback.diagnosis` 这一列 | 存了 `server.host` / `port` | 只存 `id` / `code` / `name` / `label` |

第三条还没有真的漏出去（玩家侧那两个入口只挑 `verdict` 和 `checks` 里的字段），
但它是一列会被序列化保存的数据，将来只要有人加一个"把诊断原文给玩家看"的入口，
地址就跟着出去了。改掉它是为了不留这个隐患。

### 改法

新增配置项 `servers[].code`（代号，例如 `S1`），玩家侧统一显示 `代号・名字`：

```
玩家看到：S1・生存服 1.20.1
不再显示：生存服 1.20.1 — mc.example.com:25565
```

展示逻辑收在 `server_public_label()` 一个函数里，卡片、下拉框、
以及将来的任何玩家侧入口都走它。**留空 `code` 时只显示名字** ——
老配置不改也能正常跑，不会变成「・测试服」这种开头一个点的东西。

后台服务器页仍然显示完整地址，管理员核对用户面板配置时需要它。
新增/编辑服务器的表单里多了一个「代号」输入框。

### 测试

新增 5 条 helper 行为断言 + 15 条源码级守卫。

守卫盯的是写法而不是输出 —— 模板渲染出来的 HTML 没法用命令行断言直接覆盖，
只能禁止玩家可达的三个文件（`views/player-form.php`、`views/player-ticket.php`、
`public/controllers/api.php`）里出现 `$srv['host']` / `$server['port']` 这类取值。
把改动前的那三行放回去，这 15 条会全部失败（已实测）。

锁定那条另加 10 条 `resolve_locked_server_id()` 行为断言 + 4 条源码级守卫
（盯判定有没有真的接在提交路径上 —— 抽出函数只是"可以测"，不接上去等于没修）。
修复前的代码在 3 个场景下会被这组测试判为失败，含"故意不带令牌"那条绕法。

### 顺带修掉：多台服务器时，专属链接锁不住服务器

服主不止一台服时，每台服都有一条独立的公开反馈链接（这个功能本来就有）。但链接
**只是把下拉框默认选中那一台，选项里仍然列着其他所有服，提交时后端也只看表单值**。

后果：把 A 服的链接发到 A 服群，群里任何人提交时都能把服务器改成 B 服。
系统就真去连 B 服做诊断、甚至执行修复动作，消耗 B 服的风控配额
（重启配额是按 server_id 计的），工单还记在 B 服名下 —— 服主查后台会以为
B 服真的出问题了。

现在的行为：

| 入口 | 下拉框列出什么 |
|---|---|
| A 服的专属链接 | 只有「不涉及服务器」+ A 服 |
| 直接访问首页 | 全部服务器 + 「不涉及服务器」 |

后端独立复核一次，判定收在 `resolve_locked_server_id()` 里：

- 表单回传的 `share_token`（HMAC 签名，玩家伪造不了）优先；
- 没有令牌时用 session 里的 `share_locked_server`（访问反馈页时写下的）。

**session 那一层是必需的**：只有令牌的话，拿着 A 服链接的人手工构一个不带
`share_token` 的 POST、server_id 写成 B 服，后端就没有任何依据区分他和
"从首页手选 B 服"的正常玩家。这是实测出来的绕法（见下面的测试清单）。

两个必须保留的例外：

- 「不涉及服务器」（空值）在锁定时仍然放行。玩家客户端崩了（模组冲突、
  Java 版本、内存不足）跟是哪台服无关，从专属链接进来的人同样要能报。
- 锁定的那台服已经从配置里删掉时，不当成锁定，避免把无效 id 写进工单。

已知边界：玩家禁用 cookie 时会拿不到 session，此时退化为修复前的行为
（按表单值提交），不报错。这是刻意的取舍 —— 为了堵一条不算越权的绕法
（B 服也是服主自己的服）而让禁 cookie 的玩家提交失败，不划算。

---

## [1.13.0] — 审计回合：一条 HIGH，三条加固，两条被实测否掉

四个审计员分别查了认证/会话、令牌/凭据、注入/文件、出站/执行链。**每一条结论我都自己复现过**，
下面标了哪几条不成立。

### ★ HIGH：审批闸门只罩住了六条通道里的一条

`Executor::repair()` 收了 `$needsApproval`，但**只传给了 `dispatchToAgent()`**：

```php
if ($def['exec'] === 'panel' && Rcon::enabled($server)) {
    $result = self::runRcon(...);          // 无视 $needsApproval，直接执行
}
if (... $panel->capabilities() ...) {
    $result = self::runPanelConsole(...);  // 同样
}
switch ($mode) {
    case 'local': return self::runLocal(...);
    case 'ssh':   return self::runSsh(...);
    case 'agent': return self::dispatchToAgent(..., $needsApproval);  // ← 只有这条带审批
}
```

后果：**高危配方在 RCON / 面板控制台 / 面板电源 / local / ssh 五条通道上会立刻执行**，
而工单上写着"已提交管理员确认"、管理员的待批准列表里什么都没有。玩家提交一句
"我被误封了"，走 RCON 的服务器上 `pardon <玩家>` 当场发出去。

这也推翻了我此前的一个说法。我之前说过"`unban_player` 是 high 风险所以永远要人工审批"——
**那句话只在 Agent 通道上成立**。

修法：闸门移到 `repair()` 的**最前面**，在所有通道之前拦下，统一排队等批准。
测试用真实执行路径验证：造一台配了 RCON 的服务器，要求带审批执行 `unban_player`，
断言任务数只 +1 且状态是 `awaiting_approval`（不是 done/failed）。

### MEDIUM：`pull_mod` 能被当成任意文件下载器

`api.php` 的名单来自 `analysis.needs.components[].name` 和 `suspects` —— 而这两个
**都来自玩家上传的日志**。配上双向子串匹配：

```php
stripos($name, $component) !== false || stripos($component, $name) !== false
```

玩家写一句 `Mixin apply for mod ab failed` 就能造出 2 字符的名字 `ab`，
匹配到服务端上任意一个名字含 `ab` 的 jar 并下载走。

修法三层：改成**精确匹配**（去掉双向子串）、名字短于 4 字符直接跳过、
**服务端自己的 mod 清单可用时只认它**（`cross_check.client_missing` 来自服务端清单，
不是玩家随手编的）。

### LOW：Agent 令牌接受公开仓库里的占位值

`config.example.php` 里 ship 的是 `CHANGE_ME_AGENT_TOKEN`，而 `Agent::auth()`
只拒绝空串 —— 照抄不改的部署，`?r=api.agent.*` 对任何人开放。
现在复用 `Token` 的占位值名单（新增 `isPlaceholderSecret()`，名单只有一份），
并把 `CHANGE_ME_AGENT_TOKEN` 加进去。

### LOW：后台会话永不过期 + 登出可被 CSRF

- `sessionFingerprint()` 是**全局常量**（哈希 + 后台路径），不绑定单个会话；
  `$_SESSION['console_since']` 一直在写、从来没人读 —— 被偷走的 cookie 可以一直用。
  现在加 7 天绝对有效期；老会话（没有这个键）补记时间而不是直接踢掉，升级不会让在线管理员掉线。
- 登出原来是 `GET ?p=logout`，任何页面一个 `<img src>` 就能把管理员踢下线。
  改成 POST + CSRF，侧栏按钮换成表单样式。

### LOW：下载响应头没过滤控制字符

`ModLibrary` 的 `Content-Disposition` 是全项目唯一一个只做引号替换的 `header()` 参数。
虽然上游 `safeFilename()` 已经会去掉控制字符（不可达），但这层不该依赖上游的保证。

### 两条被实测否掉的高危

**「密钥不可用 → 任意签名可过」：不成立。** 机制描述是对的（`hmac()` 在密钥不可用时
返回空串），但结论错了：空签名在 `Token.php:124` 就被拒，而 `hash_equals('', 'x')`
对非空签名是 `false`。而且 `candidateSecrets()` 早就把不可用的密钥过滤掉了，
真到不了那个分支。**已写成永久断言**（四条伪造签名各验一遍），以后谁改坏 fail-closed 会立刻发现。

**「IP 白名单可被伪造 X-Forwarded-For 绕过」：不成立。** 线上实测：带着
`X-Forwarded-For: 203.0.113.99` 登录，事件里记的仍是**真实的客户端 IP**。
原因是自带的 Nginx 配置 `include fastcgi.conf`，里面有
`fastcgi_param REMOTE_ADDR $remote_addr` —— REMOTE_ADDR 是真实客户端 IP，
不在 `trusted_proxies` 里，转发头根本不会被采信。

**「`X-Forwarded-Proto` 能去掉 Secure 标志」：也不成立** —— 实测两种请求的 cookie
都带 `secure`；nginx 设的 `HTTPS` 变量独立成立，那个头只能加不能减。

不过这三种情形在别的部署形态下**确实**是危险的（比如 TCP fastcgi 且没设 REMOTE_ADDR），
所以顺手做了两件事：把 `config.example.php` 里那段说明改成"决定因素是 REMOTE_ADDR"
（原来写的是"Nginx 怎么写"，会把人引偏），并在后台「运行信息」加了**来源 IP** 一行，
直接显示当前是从连接来源取还是从转发头取、以及后者可被伪造 —— 运维一眼能看出自己处在哪种情况。

### 验证

- **400 项单元测试全过**（本轮 +40）。
- 每条修复都有行为断言；审批闸门那条走的是真实 `Executor::repair()` 路径，
  并配了反向对照（不需要审批的动作必须照常下发）。
- 两条被否掉的结论也写成了断言 —— 它们描述的确实是危险情形，只是当前实现不成立。
- 临时副本端到端：GET 登出无效、缺 CSRF 的 POST 被拒、带令牌的 POST 正常登出。

### 仍未处理（部署层面，不在代码里）

同机还有两个与本项目无关的端口对公网开放：一个是自建的 LLM 网关（注册和调用都没设门槛，
无邮箱验证、无验证码、签到就送额度），一个是面板自带的数据库管理页面。
前者意味着任何人都能注册账号消耗上游 API 余额。这属于运维配置，已单独提示，未擅自改动。

### 顺带：简幻欢接入指南（新增文档）

补了一份[简幻欢（SimpFun / SFE4）接入指南](deploy/简幻欢接入指南.md)。

它和之前两家 Pterodactyl 系主机商**完全不是一类**，值得单独写的原因有三个：

1. **自研面板，没有开放 API** —— MCFix 五个面板适配器一个都用不上，
   所以「面板 API」那条路直接断掉，只剩 RCON 和"只做客户端分析"。
2. **RCON 的门槛不在改配置，而在端口** —— 改 `server.properties` 很容易（有文件管理和 SFTP），
   但 RCON 需要一个额外端口，而官方写明「新建端口一次性收积分」且
   「**仅对必要稳定用户提供增加端口功能**」。新号可能压根没有那个按钮。
3. **两条会直接导致误报的机制**：连续 7 天不开启实例会被**自动销毁**（表现为 `server_offline`），
   以及官方明说**晚高峰关闭实例后可能无法再启动** —— 后者意味着"自动重启"这条修复
   在简幻欢上要慎用，指南里明确建议关掉重启类配方。

顺带发现简幻欢的面板终端**可以变成交互式 shell**（官方教 tar 用法时就是这么做的：
把 `start.sh` 主命令改成 `bash`），所以 Agent 路线在机制上是通的 ——
唯一的未知数是镜像里有没有 PHP，指南里给了 `php -v` 这个 10 秒的验证办法。

**这份文档里没有一条标 ✅ 实测** —— 我没有简幻欢账号，跑不了。官方来源的标 📄，
由官方资料推出的标 🔍，查不到的标 ❓ 并列出「你自己在面板里看一圈」的清单（5 条）。
按这个项目一贯的规矩：**写错的教程比没有教程更糟**，所以宁可标着待查证，也不猜。

### 顺带：CI 一直是红的（从第一次发布起）

`.github/workflows/ci.yml` 里「确认没有把配置或运行时数据提交进来」那一步拿
**"目录是否存在"**当判据：

```bash
for path in config/config.php storage/data storage/logs; do
  if [ -e "$path" ]; then ... exit 1; fi
done
```

但 `storage/data`、`storage/logs` 因为要跟着仓库走（解压即用）**必然存在** ——
所以这步**每次都失败**，25 次 CI 全红，从第一次发布起就没绿过。

改成按文件白名单判断（`storage/` 下只允许 `.gitignore` / `.gitkeep` / `index.html` / `.htaccess`），
并顺手加了一条**全仓库找 sqlite 数据库**的检查（原来没有）。

验证：拿真实目录 + 三个反向对照跑过 ——

| 场景 | 期望 | 实际 |
|---|---|---|
| 真实仓库目录 | 通过 | ✅ 通过 |
| 塞一个运行时日志 `storage/logs/app-*.log` | 拦下 | ✅ 拦下 |
| 塞一个 `storage/data/mcfix.sqlite` | 拦下 | ✅ 拦下 |
| 塞一个 `config/config.php` | 拦下 | ✅ 拦下 |

> 第一版白名单我漏了 `storage/.htaccess`（它是**该跟着仓库走**的防护文件），
> 反向对照跑出来才发现 —— 真目录被误判成失败。这也是为什么三个对照都要跑：
> 只跑"真目录通过"会以为改对了，只跑"能拦下"会以为改严了。

### 顺带：许可证从 MIT 换成 Apache 2.0

`LICENSE` 换成 Apache License 2.0 原文，并新增 `NOTICE`。

**换的理由**：§3 那条**明确的专利授权**。MIT 只给版权许可、对专利只字未提，
贡献者默认没做任何专利承诺；Apache 2.0 把这件事写清楚了，对公司/团队用户更友好。

**代价（如实说）**：Apache 2.0 **与 GPLv2 不兼容**（GPLv3 可以）。
以后不能把这里的代码合进 GPLv2 项目。这个损失是实打实的，只是对本项目不构成影响。

**原文怎么来的**：没有凭记忆写。从 `apache.org` 取原文，并做了四重校验 ——

| 校验 | 结果 |
|---|---|
| 字节数 | 11358（canonical 大小） |
| 编码 | 无 BOM、纯 LF、零个 CR |
| **同一 URL 从两套网络各取一次** | **SHA-256 完全一致**（`cfc7749b…`） |
| 与 `apache/httpd` 仓库自带的 LICENSE 对比 | 正文逐字节相同，差异只有两处 |

最后一行的两处差异正好能证明"正文确实一样"：

- httpd 那份**少一个开头的空行**（apache.org 原文第一行就是一个空行）
- httpd 那份在正文之后**追加了自己的子组件声明**（`APACHE HTTP SERVER SUBCOMPONENTS`，
  约 335 行，是 mod_mime_magic 等第三方代码的授权）

`diff` 输出只有一个 `1d0` 和一个 `202a202,537` —— **第 2~201 行没有任何差异**，
也就是许可证正文本身逐字节相同。

> 中间出过一次假验证：我本想用 GitHub 的 licenses API 做第二来源，
> 但那次请求失败了，写出来的文件是 **0 字节** —— 而空文件的 SHA-256 和一些东西都"不相同"，
> 差点得出"两个来源不一致"的结论。加了非空判断后改从服务器网络重取，才拿到真结果。
> 教训和之前那次一样：**比较之前先确认两边都不是空的。**

**没做的事，以及为什么**：没有给 70 个 PHP 源文件逐个加 Apache 样板头。
§4 的要求是分发时带上 `LICENSE` 和 `NOTICE`，仓库根目录这两份就够了；
源码头部是 Apache 自己的"建议做法"（Appendix 里那句 "We also recommend…"），不是生效前提。
给 70 个文件各加 13 行，只会把真正的改动淹掉。

**同时更新**：`README.md` / `README.en.md` 的徽章与许可章节（含白话版"你可以 / 你要"对照表，
以及 GPLv2 不兼容这条代价）、`CONTRIBUTING.md` 新增 §5 的贡献授权说明。

> 顺手确认了**仓库里没有捆绑任何第三方代码** —— `public/assets/` 下的 JS 和 CSS 都是手写的
> （`app.js` 第 3 行的注释原话是"无依赖，纯原生 JS"），所以换许可证不牵扯任何第三方授权，
> 也不需要额外的第三方声明。

### 顺带：一个只有 git 用户才会踩到的坑 —— `storage/data/index.html` 其实提交不上去

根 `.gitignore` 里写着：

```gitignore
storage/data/*
!storage/data/.gitignore
!storage/data/index.html      # 想让这个文件跟着仓库走
```

但 `storage/data/.gitignore` 自己写的是：

```gitignore
*
!.gitignore
```

**位置更深的 `.gitignore` 优先级更高**，所以那条 `!storage/data/index.html` 从来没生效过，
`git add` 会静默跳过它。

为什么一直没暴露：本项目的发布走 GitHub API（逐个文件上传），**完全绕开 `.gitignore`**，
所以发出去的包里一直都有这个文件。只有真的 `git clone` 再 `git add` 的人才会碰到 ——
而那种情况下 `storage/data/` 会缺一层 `index.html`（它的作用是阻止目录被直接列出）。

是这次改用 git 推送发布时才冒出来的：`git ls-files` 数出 123，扫描却是 124。

修法是给 `storage/data/.gitignore` 补一行 `!index.html`，让两份 `.gitignore` 的意图一致。

### 顺带：手机端适配（玩家页 + 管理后台）

起因是反馈「没有适配手机端、加载巨慢」。查下来是**两件互不相干的事**，分开处理。

**先纠正我自己一个错误判断。** 第一张手机截图看起来「内容被右侧截断」，
我据此认为布局超出视口 —— **那是测量假象**：Chrome 的窗口最小宽度被钳制在约 492px，
`--window-size=390` 实际按 492px 排版，再把截图裁到 390px，看起来就像布局太宽。
用 iframe 拿到真正 390px 视口后实测 `scrollWidth == clientWidth`，**没有横向溢出**。
教训：**截图不能当布局证据，要看数字。**

390px 下真正的问题是这些：

| 问题 | 原来 | 现在 |
|---|---|---|
| **iOS 自动缩放** | 输入框 `font-size: 14.5px` —— iOS 聚焦时会**把整页放大且缩不回去** | 窄屏 16px |
| **触摸目标偏小** | 输入框 42px、chip 约 34px | 46px |
| **并列输入框不换行** | `.field-row` 没有 `flex-wrap` | `flex: 1 1 180px` 自动换行 |
| **GPU 开销** | 每张卡片 `backdrop-filter: blur(6px)`、两个 `position:fixed` 全屏渐变层、呼吸灯无限动画 `box-shadow` | 卡片去掉模糊、固定层改为随文档绘制、呼吸灯改用 `transform/opacity` |

呼吸灯那条值得单说：动画 `box-shadow` 是**绘制**属性，动画它等于每帧重绘，
而它是 `infinite` 的 —— 页面只要开着就永远按 60fps 重绘。桌面看不出来，手机上就是掉帧发烫。

**后台：一个"手机上进不去工单"的 bug。**

工单表 7 列，而 `.table-card` 是 `overflow: hidden`。492px 下实测：

```
最后一列右边缘 = 696    卡片右边缘 = 476
=> 220px 的内容被永久裁掉，而且没有滚动条
```

也就是手机上**看不到工单的「状态」「时间」，也点不到「查看」** —— 等于打不开工单。
改成窄屏横向可滚 + `.table { min-width: 660px }`（否则 7 列会被压成一字一行）。
修复后程序化验证：`scrollWidth 476 → 695`，滚动后最后一列可达。

> 这个 bug 线上**看不出来** —— 当时 0 工单，页面是空状态。是拿真实标记 + 三条假数据
> 渲染出来才发现的。**空状态会掩盖表格类问题**，这点以后要注意。

**后台导航：7 个项摊成三行 → ☰ 折叠菜单。**

原来窄屏把 7 个导航项横排折成三行，占掉近 200px 高度。改成「品牌 + 退出 + ☰」一条窄栏。

菜单的边界很明确：**里面只放导航按钮，竖着一行一个**（7 个 × 约 44px ≈ 310px，
在手机上约占屏高 1/3）。展开时三条杠收成 ✕，状态一眼可见。

第一版我把侧栏底部的统计和链接也塞进了菜单里，那是错的 —— 导航是"去哪儿"，
统计是"现在什么状态"，两者混在一起之后，手机上既看不到状态、菜单也变长了。
现在：

| 内容 | 位置 |
|---|---|
| 7 个导航按钮 | ☰ 菜单里，竖排 |
| 待执行任务 / 待处理工单 / Agent 在线 | **工单页顶部**（工单页就是后台主页） |
| 退出登录 | 窄屏放顶栏，桌面在侧栏底部 |
| 玩家反馈页链接 | 桌面侧栏底部 |

桌面侧栏那三个数字也一并去掉了 —— 否则桌面端打开工单页会同时显示两遍同样的数字。
现在这三项只有工单页一个出处（导航上仍保留待处理工单的角标）。

**用 checkbox + `<label>` 实现，不引 JS** —— 后台脚本一旦没加载，导航就彻底点不开了，
而它是进每个页面的唯一入口。桌面端 `.side-collapse` 用 `display: contents`，
让子元素直接当侧栏的 flex 子项，**布局与改动前一致**（已截图核对）。
键盘可达：checkbox 只做视觉隐藏（**不能用 `display:none`**，那样就聚焦不到了）。

### 顺带：HTTP/3（部署层面，不在代码里）

反馈「丢包很严重」，实测确认：**ICMP 50 包丢 6 个 = 12% 丢包，而 RTT 中位数只有 24ms。**

**问题不是"远"，是"丢"。** 12% 对 TCP 是灾难：丢一个包就等重传超时（初始 1s 起），
而 HTTP/2 跑在 TCP 上，**一个包丢了会把这条连接上所有请求一起卡住**。
之前测到的 1.0 / 1.8 / 4.1 秒停滞就是这么来的 —— 不是服务器慢（服务器稳定 0.15s），
是 TCP 在替丢包背锅。

启用了 HTTP/3（nginx 编译带了 `--with-http_v3_module`），并**验证它真的从用户网络跑通**：
nginx 访问日志里出现了 `"GET /index.php HTTP/3.0" 200`，来源 IP 与测试机一致
（具体 IP 属于用户隐私，不写进仓库）。
说明这条链路 UDP 没被墙 —— 这也是之前不敢开 h3 的唯一顾虑。

同时打开 `ssl_session_tickets`（原来是 `off`）：它会**同时禁用 TLS 1.3 和 QUIC 的会话恢复**，
每次访问都要完整握手，而 12% 丢包下一次握手有 30%+ 概率吃到重传停滞。

> **没能做成的事**：「TCP vs QUIC 加载耗时对比」跑出 TCP 中位数 29ms、最小 20ms ——
> 这个数字不可能是真的（光启动浏览器就不止 20ms），说明 Edge 在页面加载完成前就返回了。
> 所以**没有引用这组数据**。能确证的只有 h3 协商成功；快了多少要靠真机体感。

### 新增：站点图标与邮件 Logo（可在后台更换）

两件事合成一个功能做，因为它们是同一类东西 —— "一张属于这个站点的图片"，
只是槽位不同（`favicon` / `logo`）。实现见 `src/Brand.php`。

**站点图标**：后台「系统设置 → 站点图标」上传 PNG/JPG/GIF/WebP，
浏览器标签页上那个小图标就换成你的。没上传时仍是内置的 🧱（内联 SVG，零额外请求）。

**邮件 Logo**：上传后发信时显示在邮件正文顶部。

**为什么用 Content-ID 内嵌，而不是放一个图片地址：**
绝大多数邮件客户端**默认不加载远程图片**（防跟踪），收件人看到的是灰色占位框，
还得手动点"显示图片"。内嵌的图是邮件本体的一部分，不触发这个限制，打开就有。
代价是每封邮件大一点（就是图片体积）。

MIME 结构相应多包一层：

```
multipart/related
  ├─ multipart/alternative   ← 正文（纯文本 + HTML）
  └─ image/*  Content-ID: <mcfix-logo>
```

**没传 Logo 时 `buildMessage()` 的输出与加这个功能之前同构**（仍是
`multipart/alternative`）—— 不会因为多了个可选功能就让所有老部署的邮件格式变样。
测试钉住了这一条。

**几个刻意的取舍：**

| 决定 | 原因 |
|---|---|
| **不支持 SVG** | SVG 能内嵌 `<script>`，而它会被同源地发出去 —— 直接访问那个 `.svg` 就是一次存储型 XSS。图标用 PNG 完全够。 |
| 按文件头判断类型，不信扩展名和 Content-Type | 后两者都是客户端说了算的。用 `getimagesize()` 真解析一遍。 |
| 体积用真实 `filesize()`，不用 `$_FILES['size']` | 那个字段同样来自客户端。 |
| 存 `public/brand/`，不走 PHP 路由 | favicon 每次页面加载都要取一次，交给 nginx 直接发静态文件，既快也不占 PHP 工作进程。 |
| 不加配置项记文件名 | **文件在不在本身就是"有没有设置"**。多一个配置项就多一个能对不上的地方（文件删了配置还在）。 |

**顺手补的一个发布闸门**：`public/brand/` 里是**这套部署自己的**图标和 Logo，
不属于项目代码。发出去既泄露部署者的品牌素材，也会在别人解压后把人家的图标覆盖掉。
所以 `publish-github.ps1` / `publish-from-server.py` / `build-pkg-local.ps1`
三处都加了 `public/brand/` 白名单（只放行 `.gitignore` / `index.html`），
发布脚本里还加了一道硬闸门：**检测到真图片就直接中止发布**。

**关于"邮件发信头像"要说清楚的一件事**：这个功能改的是**邮件正文里**的 Logo。
收件箱列表里发件人旁边那个头像，图片是客户端按发件地址自己决定的（Gmail 看 Google
账号、QQ 邮箱看 QQ 号），**发信方控制不了** —— 除非去申请 BIMI
（需要付费的 VMC 证书 + DMARC 强制策略）。后台那段说明里也写了，
免得有人传了 Logo 发现列表里没变化，以为功能坏了。

**测试**：新增 18 条，400 → 425。其中一条**第一次是失败的**，而它暴露的是可测性问题
而不是功能问题：`store()` 第一句 `is_uploaded_file()` 只对真正的 multipart 上传返回
true，测试里造不出这个条件 —— 于是"SVG 被拒绝""非图片被拒绝"两条**永远测不到**
（实际撞到的是"上传校验失败"）。修法是把校验拆成独立的 `Brand::inspect()`，
`store()` 只负责"是不是上传来的"和"存到哪"。拆完才测得动，也顺手补了一条反向对照：
**真 PNG 必须通过** —— 否则前面那些"拒绝"可能只是拒绝了一切。

---

## [1.12.9] — 把「日志留存」拆成独立卡片（外加一次自己造成的 500）

### 问题

1.12.6 加了日志留存天数的输入框，但它被放在「玩家反馈」这个网格的**第 6 项、
最后一项**。管理员连着两轮反馈"找不到" —— 说明位置就是错的。

回头看那个位置确实不合理：这是一条**会删玩家数据**的设置，藏在一堆限流参数的最后，
标题还是「玩家反馈」，而它旁边五项都是"链接有效期""每 IP 每小时提交上限"这类东西。

### 做法

拆成右下角一张**独立卡片**，标题就叫「日志留存」：

- 标题带"日志"两个字，扫标题就能找到
- 输入框 + **「保存留存天数」按钮**（原来只有输入框，没有按钮，和"找个按钮"的预期不符）
- **「立即清理一次」按钮**也搬了过来，不用再去「维护工具」里找
- 现状数字（库里 N 条还留着原文、已清理 M 条）跟着卡片一起走
- 主表单里留一句指引，告诉从「玩家反馈」找过来的人去哪

### 一个必须独立动作的理由

不能简单地把字段挪到卡片里、还让主表单提交 —— `admin_save_settings()` 是
**整表单覆盖**语义，缺字段就取默认值。一张只带 `log_retention_days` 的小表单提交上去，
会把**其它字段一起重置**。

为此新增独立动作 `admin_save_log_retention()`，只碰 `feedback.log_retention_days`
一个键。缺字段时明确报错而不是当成 0（0 是有含义的值 = 不清理，不能和"没填"混淆）。

### 顺带修掉一个一直存在的隐患

同一个"整表单覆盖"语义下，`admin['ip_allow']` 原来也是无条件重写的：

```php
$ipRaw = param($input, 'console_ip_allow', '');   // 字段缺失 → 空串
$items['admin']['ip_allow'] = $ips;               // → 空数组
```

也就是说，**任何一次不带该字段的局部提交都会把后台 IP 白名单静默清空** ——
那是在放松访问控制，而且无声。现在加了 presence 守卫（和 `domains` 两字段同样的处理）。

### 这次部署把线上打挂了 3 分钟，根因和修复

部署自检里有一项"点一次保存，确认只改一个键"，它是**以 root 通过 CLI** 调
`admin_save_log_retention()` 的。`Config::save()` 写临时文件再 rename，
于是新文件是 **`root:root 0640`** —— 而 php-fpm 以 **www** 运行，读不到
`config/config.php`，**两个域名同时 500**。

已恢复（`chown www:www`），并修掉根因：`Config::save()` 现在把新文件的属主
**对齐到配置目录的属主**，而不是跟着"谁在跑"。

这个隐患不止自检会踩 —— root 的 crontab 里跑 `mcfix cron`、运维手敲一条
`php -r` 调后台动作，都会产生同样的后果。

正负对照验证：

| | 保存后 config.php 属主 |
|---|---|
| 旧代码（负向对照，复现 bug） | `root:root 640` ← 就是它打挂了站点 |
| 新代码 | `www:www 640` ✓ |

同一条门禁已加进部署脚本；部署脚本末尾也加了 `chown -R www:www config storage` 兜底
（以 root 跑单元测试会在 storage 下留下 root 属主的文件）。

### 验证

- 新增 15 条测试（**359 项全过**），其中最关键的三条断言独立动作**没有碰**
  站点名 / 链接有效期 / 后台 IP 白名单。
- 另一条专门守白名单那个隐患：先设两项白名单，再提交一次不带该字段的请求，
  断言白名单原样还在。
- 还有一条断言 `name="log_retention_days"` 在整个设置页里**只出现一次** ——
  防止以后有人把它加回主表单，变成两个输入框互相覆盖。
- 临时副本端到端：卡片渲染出标题、输入框、两个按钮和现状数字；点「保存留存天数」
  真的写进 9 天，且站点名、后台路径、白名单全部原封不动。

---

## [1.12.8] — 日志里不该出现的三类东西

起因是审计了一遍"我们自己写出去的日志"，找到三处真实泄露。

### 一、nginx 访问日志把反馈令牌写了下来

玩家的反馈链接是 `?r=feedback&t=<签名令牌>`，而 nginx 默认的 `$request` /
`$request_uri` 会把整串写进 access log：

```
"GET /?r=feedback&t=<令牌> HTTP/2.0" 200 1986
"GET /?r=api&action=progress&token=<令牌> HTTP/2.0" 200 2837
```

**谁能读到 `/www/wwwlogs/`，谁就能在令牌有效期内直接打开那个玩家的工单。**
日志会被备份、被日志采集服务收走、被顺手 `cat` 出来 —— 它不该是一份可用的凭据清单。

改法：反馈站改用不含查询串的日志格式（`$uri` 代替 `$request`），
排查接口和状态码完全不受影响。后台站 URL 里没有令牌，保持原样。
模板已写进 `deploy/nginx.conf.example`，新部署默认就是安全的。

### 二、事件表里存了完整邮箱

「测试邮件」把收件地址原样写进 `events` 表：

```
email.test: 测试邮件：已通过 SMTP 投递给 player@example.com
```

这个项目对玩家邮箱一直是掩码处理的（`purgePlayerEmail` + 表单上明说"不会长期留存"），
管理员自己的地址却在审计记录里留了明文 —— 标准不一致。
现在写事件前过一遍 `maskEmailsInText()`，页面上看到的仍是完整地址（刚填的，需要确认），
永久记录里是 `pl***@example.com`。

### 三、`app_log` 靠调用点自觉，没有兜底

上游服务经常把密钥原样回显在报错里（`Invalid API key: sk-...`），
这类文本会被一路带进 app 日志和 events 表。当时检查下来没有泄露，
**但那是运气，不是设计** —— 任何一个新调用点都可能带进去。

新增 `redact_secrets()`，在 `app_log()` 里兜一道。**刻意只抹凭据，保留 IP 和邮箱**：

- 抹：`sk-…` / `ghp_…` / `Bearer …` / `api_key=…` / ≥40 位的长随机串
- 留：IP、邮箱

因为日志里"哪个 IP 被白名单拒了""哪个 IP 登录失败"是排查时最有用的一条信息，
抹掉日志就没法用了。全面脱敏应该发生在调用点（`TicketMail` 自己会用 `maskAddress`），
不在这里搞一刀切 —— 有一条断言专门钉住这个取舍，防止有人"顺手改成"全面脱敏。

`AiAdvisor::redact()` 现在复用同一个函数，不再各维护一套密钥规则。

### 顺带修了一个测试守卫的误报

"无命名空间的文件里没有忘了 use 的类引用"这条守卫会扫注释 ——
我在注释里写了一句 `见 TicketMail::purgePlayerEmail` 就被它判成"忘了 use"。
注释不是代码引用。现在扫描前会先把注释换成等长空格（行号不变）。
这是同一类毛病的第三次：**断言被散文触发**。

### 验证

- 新增 12 条测试（**344 项全过**）。
- `app_log` 那几条是**行为测试**：真写一条带密钥的日志，读回新追加的那一段断言。
  `storage_path()` 指向项目真实目录，所以测完会把文件截回原长度，并断言没有留下痕迹。
- nginx 改动带备份 + `nginx -t` + reload + 真实请求验证（发一个带标记令牌的请求，
  确认新日志里没有它），任何一步失败自动还原。

---

## [1.12.7] — 大模型报错别再切一半，再加一个"问接口要模型名"

### 起因

实测「测试大模型」时报错长这样（模型名和 request_id 换成了**等长**占位符，
下面"截断在 200 字"的演示位置才能照原样复现）：

```
调用失败（196 ms）：HTTP 400：{"error":{"message":"The supported API model names are
example-llm-01, example-llm-002, but you passed Example-LLM-001-Pro. (request_id:
00000000-0000-4000-8000-000000000000)","type":"invalid_request_。检查接口地址是否带
/v1、密钥是否正确、模型名是否存在。
```

**上游已经把答案写清楚了**（"支持的是 example-llm-01 / example-llm-002"），
但界面上只剩一段断在中间的 JSON 碎片，还挂了一句答非所问的通用提示。

两个缺陷：

1. `mb_substr($raw, 0, 200)` 把错误体硬截到 200 字 —— 正好切在 `"type":"invalid_request_`。
2. 无论上游说没说清楚，都无条件追加"检查接口地址是否带 /v1、密钥是否正确、模型名是否存在"
   —— 在已经给出具体原因时，这是噪音，还会把真正的答案淹掉。

### 做法

**① 能解析就解析，别截断。** 新增 `describeHttpError()`：OpenAI 兼容接口的错误体形如
`{"error":{"message":"...","type":"..."}}`，优先取 `error.message` —— 那里面通常已经
写明"模型名不对 / 密钥无效 / 余额不足"。解析不出来（网关的 HTML 错误页等）才退回原文，
且会剥掉 HTML 标签、放宽到 300 字。返回值带上 `specific` 标记。

**② 通用提示只在"上游没说清楚"时才出现。** 有具体原因时改成提示去点「获取可用模型」。

**③ 新增「获取可用模型」按钮。** 模型名是最容易填错、又最难自查的一项 —— 各家命名
五花八门（大小写、连字符、日期后缀、`-pro` / `-flash` 各种拼法混着来）。OpenAI 兼容接口
普遍实现了 `GET /v1/models`，直接问它比让人翻文档靠谱。拿到清单后会标出**当前填的那个
在不在列表里**，不在就明说"多半就是它导致 400"。

**④ 顺手补一个泄露面。** `redact()` 原来不认 API 密钥形态，而上游有时会把密钥回显在
报错里（`Invalid API key: sk-...`）。这条消息会进 `events` 表和后台界面，现在
`sk-` / `ghp_` 等形态一律打成 `<key>`。

### 验证

- 新增 13 条测试（**330 项全过**），其中一条以那条 400 的原文为输入样本
  （长度和截断位置照原样，只把模型名、request_id 换成等长占位符），断言可用模型名
  必须出现在给管理员看的文案里 —— 旧代码截断后这句会丢。
- 另外覆盖：`error` 是字符串、顶层 `message`、HTML 错误页（要标成"不具体"并剥标签）、
  空响应体、以及**报错文案不回显密钥**。
- 「获取可用模型」实测有效：`GET /v1/models` 返回的清单里，能正确指出当前配置的
  模型名不在其中。
- 设置页实测渲染出新卡片（`<h2>可用模型</h2>` + `value="ai_models"` 按钮），无 PHP 错误。

---

## [1.12.6] — 日志留存设置补上界面

### 问题

1.12.4 加了 `feedback.log_retention_days`（归档满 N 天自动清空日志原文），
**但只写了配置项和文档，没做界面** —— 想改这个天数只能手编 `config/config.php`。
一个会**删玩家数据**的开关藏在配置文件里，既不显眼也不该要求管理员去改代码。

### 做法

后台「系统设置 → 玩家反馈」里新增一项：

- **日志原文留存（天，0=不删）** —— 数字输入框，范围和默认值都写在界面上
- 下面一行说明写清楚它删什么、留什么（原文删掉，诊断结论和 MOD 列表保留）
- **看得见效果**：直接显示"当前库里 N 条还留着日志原文，已按设置清理过 M 条"，
  设为 0 时额外提示"现在是'不清理'，原文会一直保留"
- 「维护工具」里新增 **清理日志原文** 按钮，不用干等计划任务

### 一处刻意的写法差异

`admin_save_settings` 里其它字段都是"缺了就取默认值"（`int_param($input, 'x', 默认)`），
这一条**必须**判断字段有没有提交：

```php
if (array_key_exists('log_retention_days', $input)) { ... }
```

因为这个值决定要不要**删玩家数据**。一次不带该字段的局部提交（curl 调接口、
脚本单独提交某几个字段）如果把它重置成默认的 30，就会在管理员毫不知情的情况下
开始自动删日志 —— 静默的破坏性变更。缺字段时保持原值。

### 验证

- 新增 11 条测试（**317 项全过**）。留存那组测试之所以能直接调
  `admin_save_settings`，是因为 `tests/run.php` 的 `MCFIX_CONFIG` 指向临时文件，
  保存只写临时配置，碰不到真实 `config.php`。
- 其中最关键的一条：设成 3 之后，再提交一次**不带该字段**的局部请求，
  断言值仍是 3 —— 而不是被重置成 30。少了这条，"静默开始删日志"就没人守。
- 另有三条边界：0 可用、负数夹到 0、超大值夹到 3650。
- 临时副本端到端：安装一个干净站点 → 登录后台 → 设置页确实渲染出新字段
  （`name="log_retention_days" value="30" min="0" max="3650"`）→ 造两条工单
  （一条已归档、一条未归档）→ 点「清理日志原文」→ 已归档的原文变 NULL、
  **未归档的一字未动**、事件流记录"清理日志原文 1 条"。

  这一步里我自己踩了两次坑，都不是产品问题：副本里没删 `config.php` 导致
  登录用的是线上口令；以及私有路径模式下页面地址是 `?r=<path>&p=settings`，
  不是域名模式的 `?p=settings`。

---

## [1.12.5] — 后台输入框"上下不齐"：两个真实原因

### 现象

后台设置页里，同一行的填空框有高有低。

### 原因一：52 个输入框根本没拿到样式

CSS 写的是：

```css
input[type=text], input[type=password], input[type=number], input[type=search], select, textarea { ... }
```

**属性选择器匹配的是 HTML 属性，不是 DOM 属性。** `<input>` 不写 `type` 时浏览器照样
按 text 渲染，但 `input[type=text]` 匹配不到它 —— 这些框完全拿不到内边距、边框、
圆角、`width: 100%`，只剩浏览器默认的 **21px** 高，而旁边写了 `type` 的框是 **42px**。

实测线上设置页 31 个控件，高度分别是 21 / 42 / 44 三种：

| 字段 | 写法 | 高度 |
|---|---|---|
| `max_fix_attempts` | `<input type="number">` | 42px |
| `site_name` | `<input name="site_name">` ← 漏写 type | **21px** |
| `timezone` | 同上 | **21px** |

项目里共 **52 处**漏写 `type`，另有 **4 处** `type="email"`（玩家反馈表单的邮箱框）
也不在覆盖列表里 —— 一共 56 个框没有样式。

修法是在选择器里补上 `input:not([type])` 和 `input[type=email]`，一处修好全部，
以后新增模板也不会再漏。

### 原因二：`select` 天生比 `input` 高 2px

`select` 的内容盒按 option 的行盒算，即使 padding / font-size 完全相同，
它也比 `input` 高 2px（42 vs 44）。同一行并排时上边缘齐、下边缘差 2px。
给两者锁一个统一高度。

### 差点踩的坑

`input:not([type])` 会让基础规则**也匹配到 `.copy-field`**（复制链接那种紧凑框，
原来是靠"没 type 所以匹配不到"才保住自己的样式的）。而 `.copy-field` 的元素特异度
（0,1,0）低于基础规则（0,1,1），会被直接盖掉 —— 虚线边框和等宽字体全丢。

两处对应处理：

- `.copy-field` 改成 `input.copy-field`（特异度同为 0,1,1，靠源码顺序取胜）；
- 显式声明 `height: auto`，不受统一高度约束。

也没有用 `input:not([type]):not(.copy-field)` 那种写法：它会把特异度抬到 0,2,1，
反过来盖掉 `.filter-form input { width: auto }` 之类的既有规则。

### 验证

- 用无头 Edge 加载**改过的真实 CSS**：300 条规则全部解析通过，无语法错误。
- 逐个量高度：裸 input / email / number / select **全部 42px**，
  `copy-field` 保持 34px + 虚线 + 等宽字体。
- 新增 6 条测试（**306 项全过**），其中最关键的一条是：**扫出项目里实际用到的每个
  `input type`，断言它要么被 CSS 覆盖、要么在豁免名单里** —— 以后新增
  `type="date"` 这种会立刻被拦下，而不是又悄悄变成 21px。
  这条断言自带"扫到了类型"的前置检查，否则正则失效时它会静默空转。

---

## [1.12.4] — 日志原文的留存期：从"永久保留"改成"到期自动清"

### 问题

玩家上传的崩溃报告里含 **Windows 用户名**（`C:\Users\<名字>\AppData\...`）、显卡型号、
游戏 ID、服务器地址、完整 MOD 列表 —— 都是个人信息。

而在这之前**没有任何东西删过它**。全项目唯一的 `DELETE FROM` 在 `Db::delete()`，
唯一的调用点是 `Workflow.php:506` 清理 `client_issues`（重新分析时清旧结论）。
工单和日志永久保留：没有 TTL、没有留存设置、后台也没有"删除工单"入口。

对比之下**邮箱一直有清理机制**（`TicketMail::purgePlayerEmail`，工单结束后换掩码，
写 `email_purged_at`，记 `email.purged` 事件，表单上还向玩家明说了）。这个不对称是漏的。

更糟的是**文档里一个字都没提** —— README、安全说明、`config.example.php` 都没说，
玩家以为链接 24 小时过期就没人看了，实际上原文一直躺在库里。

### 做法

新增 `feedback.log_retention_days`（**默认 30**，`0` = 关闭该功能）：

> 归档满 N 天后，cron 把 `client_log` / `client_log_name` 置空，
> **诊断结论、MOD 列表、客户端分析结果全部保留**。留着有用的，删掉没必要的。

安全边界（每条都有测试盯着，其中一条是负向对照）：

- **只动 `status = 'closed'` 的工单。** `resolved` 或进行中的可能还要重新分析，
  原文删了就分析不了 —— 有一条负向对照专门守这个边界（造一条 60 天没动但
  **未归档**的工单，断言它的原文没被碰）。
- `log_retention_days = 0` 时一条都不动。
- 已经清过的（`log_purged_at` 非空）不重复处理。
- 每次清理记一条 `log.purged` 事件，玩家在工单页也能看到"原文已按留存设置清理"，
  而不是发现日志莫名其妙不见了。

新增 `log_purged_at` 列（`Db::addColumnIfMissing`，老库自动补）。

### 文档

- README / README.en.md 新增「数据留存」小节：邮箱和日志各自的清理时机、配置项、
  以及 `0` 关闭时的注意事项。
- 安全说明 / SECURITY-NOTES 第五节补上留存期，并写明"在 1.12.4 之前没有任何东西
  删过它"这个事实。
- 反馈表单上那句隐私提示原来只说邮箱会被掩码，现在把日志留存期一起说清楚
  （数字从配置里读，不是写死的）。

### 验证

- 新增 10 条测试（**300 项全过**），含最关键的那条负向对照。
- 临时副本端到端：造一条 60 天前归档的工单 + 一条同样老但未归档的工单，
  跑 `php bin/mcfix.php cron` —— 归档的原文变 NULL、结论保留、清理时间戳写入、
  事件入库；**未归档的原文一字未动**。
- 表单页实测渲染正常（视图里新调了 `Config::get`），无 PHP 错误/警告。

---

## [1.12.3] — 修好仓库里指向自己的三个链接

`.github/ISSUE_TEMPLATE/config.yml` 里三处仓库地址一直是 `https://github.com/OWNER/REPO`
占位符，从没替换过。后果是 Issue 模板里的"安全漏洞（请勿公开）"和"部署文档 /
常见问题"两个链接**指向不存在的位置** —— 而 `SECURITY.md` 明确承诺了私密上报渠道，
等于承诺了一个打不开的入口。

现在填成 `https://github.com/lango578/mcfix`，并把顶部注释改成对 fork 的人更有用的
说法（原来的注释是让克隆者替换占位符，现在占位符没了，那句话会自相矛盾）。

无源码改动。

---

## [1.12.2] — 动作核实闸门：玩家上传的文本不能单独指挥系统

### 问题

客户端判定那一侧的**全部证据都是玩家上传的文本** —— 玩家想写什么就写什么。
而 `Workflow::mergeClientIntoVerdict()` 原来是让客户端结论**优先**，并直接接上
它的动作计划（`Workflow.php:600`）。服务端自己核实出的白名单结果**无法否决**
客户端的主张，只能追加自己的。

实测（`mergeClientIntoVerdict` 是 public 的，docblock 写明就是给测试验安全属性用的）：

| 服务端状态 | 合并后 | 动作 |
|---|---|---|
| `healthy`（玩家其实已在白名单里） | `not_whitelisted`（source=client_log） | **`whitelist_add`，`auto_fixable=true`，无需审批** |
| `need_info`（白名单核实被跳过） | `not_whitelisted` | **`whitelist_add`，无需审批** |
| `server_offline` | `server_offline` | 无 ✓ |

也就是说：**任何能打开反馈页的人，填一个名字 + 一段含
`You are not whitelisted on this server!` 的日志，就能把那个名字加进白名单。**
公开表单成了白名单旁路。同类还有 `reload_plugins`（medium，可自动执行）。

### 做法

`whitelist_add` 被定为 medium 风险是设计选择（README 也把"自动加入白名单"
写成卖点），所以这次不动风险等级，而是加一道**核实闸门**：

> 客户端主张的每一个服务端动作，必须在**服务端自己独立给出的配方清单**里
> 出现过，才算被核实。核实不到 → 仍然列出来给玩家看，但强制走人工审批，
> 不再自动执行。

服务端核实过的动作照旧自动执行，功能没被砍掉；核实不到的从"直接动手"
降级为"等管理员点一下批准"。

### 一处需要说明的取舍

**没有**顺手把服务端的配方合并进最终清单（现在客户端结论胜出时，
`$verdict['suggestions']` 会被客户端清单覆盖）。那会让**更多**动作被执行，
方向与"收紧"相反，属于另一个决定，留待单独评估。

### 验证

- 新增 10 条测试（**290 项全过**）：
  - 未核实 → `auto_fixable=false`、`requires_approval=true`、仍然列出（不静默丢弃）；
  - **负向对照**：服务端核实过时仍然 `corroborated=true`、不需审批、可以自动执行
    —— 确认这道闸门没有把正常功能一起砍掉；
  - 服务端故障时保持服务端结论、不采纳客户端动作；
  - 伪造日志 + 服务端已确认玩家在白名单里 → 依然要求审批。

---

## [1.12.1] — 误报：1.12.0 把玩家聊天内容当成了系统状态

1.12.0 修的是**漏判**（该认出来的没认出来），但扩大了扫描范围却没检查
**误报**（不该认的认了）。上线后专门做了一组误报探针，10 例里 6 例确凿误报，
其中 3 例是 1.12.0 引入的回归：

| 玩家实际做的事 | 系统告诉他 | 还附带 |
|---|---|---|
| 在公屏问「白名单怎么加啊」 | 你不在服务器白名单里 | 「系统会自动把你加进白名单」 |
| 说一句「他昨天被封禁了吧」 | 你的账号处于封禁状态 | 自动提交解封请求 |
| 聊天里出现 `server is full` | 服务器已满员 | — |

误报比漏报糟得多：漏报是"我不知道"，误报是一个**明确而错误的结论**，
玩家会照着它去做没用的事，第二条甚至会被系统当成真的封禁去申诉。

### 根因与修法

**① 聊天行被当成系统状态。** `[CHAT] <Steve> 白名单怎么加啊` 和
「服务器告诉你被踢了」在文本上一模一样。现在特征扫描前先滤掉聊天行：
`[CHAT]` 标记，以及 `<玩家名> 内容` 形式（有些模组不走 `[CHAT]`）。

**② 三个正则在拿裸词当证据。** 收窄到有上下文才算：

- `corrupt` → 必须与 chunk / region / world / level 同时出现。
  原来「Corrupted **config** file」会告诉玩家「存档区块损坏」。
- `timed out` → 只认 `Read timed out` / `Connection timed out` /
  `SocketTimeoutException` 这类网络措辞。原来「Chunk load **timed out**」
  会告诉玩家「连接超时，先关掉加速器」。
- `certificate` → 只认 `SSLHandshakeException` / `PKIX` / `CertificateException`
  这类真正的 TLS 异常类型。原来「Invalid **certificate** in resource pack」
  会告诉玩家「HTTPS 证书问题，校准系统时间」。

### 验证

- 新增 10 条测试（**280 项全过**）：7 条误报防护 + 3 条**真阳性对照**
  （收窄正则不能把该认的一起收掉：真的区块损坏、真的网络超时、真的 TLS 失败）。
- 误报探针独立成脚本，作为部署门禁：**确凿误报必须为 0**，否则自动回滚。
  探针里另有两例标为「可辩护」而不是误报 —— 模组确实要求了不兼容的 API 版本、
  崩溃确实发生在渲染线程。把可辩护的也算成误报，会逼着代码去删掉正确的结论。
- 1.12.0 的成果没有回退：同一批 14 份日志仍是 **11/14**。

---

## [1.12.0] — 本机判定引擎：把"没有大模型"时的漏判补上

线上 `ai.enabled=false`，所以本机引擎**就是**主路径。拿 14 份真实形态的日志实测，
9 份能定论、5 份落进"日志太短 / 转人工"。逐个查下来是 4 个独立缺陷。
### ① 按 INFO 打的决定性消息扫不到

`parseGameLog()` 只把 **ERROR / FATAL / WARN** 行喂给特征扫描，`parseCrashReport()`
只喂堆栈。可是真实客户端日志里，"你不在白名单 / 你被封禁 / 服务器已满 /
客户端过旧 / 登录会话失效" 全是 **Render 线程按 INFO 打的**（只有 Netty 那层报 ERROR）。

对照实验（同一句话，只改日志级别）：

| 日志 | 旧结果 |
|---|---|
| `[INFO]: You are not whitelisted on this server!` | 无信号 → `need_log`（回玩家"日志内容太短"）|
| `[ERROR]: You are not whitelisted on this server!` | `whitelist_kick` → `not_whitelisted` ✓ |

现在解析完各路径后，会对**整份日志**再扫一遍，但只针对一组高精度特征
（`LINE_WIDE_SIGNALS`）。刻意**不含** `timed_out` 与 `ssl_error` —— 它们的正则里
有 `timed out`、`certificate` 这类宽泛词，铺到整份日志上会产生误报；
这两条仍然照常从 ERROR/WARN 行里扫。

### ② 四个"死信号"和两条"不可达条目"

特征扫得出来，`detect()` 里却没有对应分支：

- 死信号：`auth_failed`、`ccompat_error`、`datapack_error`、`modmenu_missing`
- 知识库里永远产不出来的条目：`auth_failed`、`modpack_conflict`、`server_mod_missing`

处理方式分三种，没有一刀切：

- `auth_failed` 接通（知识库条目本来就在，只是没接上）。
- `ccompat_error` / `datapack_error` 补知识库条目并接通。
- `modmenu_missing` **删掉**：正则是 `/modmenu|Mod Menu/i`，任何提到 Mod Menu 的
  INFO 行都会命中，接通等于制造误报。
- `modpack_conflict` **删掉**：内容和 `modpack_conflict_server` 重复。
- `server_mod_missing` **删掉**：内容有价值，但客户端侧我找不到可靠的签名。
  不为了凑数编一条正则 —— 写错的结论比没有结论更糟。将来拿到真实样本再加回来，
  并且要带测试。

### ③ "日志太短"会对不短的日志说

兜底阈值是 `bytes < 2000 || lines < 15`。一份 **40 行**、1826 字节的崩溃报告
因为字节数没到 2000，被判"日志内容太短，无法定位" —— 文案和证据自相矛盾。

改成 `&&`：只有**又短又少行**才算真的没信息。长但认不出来的日志归 `unknown`，
它的文案本来就是准确的（"日志里没有匹配到已知的错误特征"）。

再加一层：**已经识别成崩溃报告的，一律不判"太短"**。玩家交上来的就是完整的
crash-report 文件，回他"请把完整的崩溃报告补发一次"是错的建议。短日志判定只针对
没法确认类型的文本片段（那种多半是从截图里抄了几行）。

### ④ 显卡驱动问题必须搭上别的条件才生效

`graphics_driver` 原来要求同时出现 `render_error` 信号或 `-- Problematic frame --` 段。
于是 `Failed to create window` 这种明摆着的驱动/OpenGL 初始化失败，反倒什么结论都不给。

新增高精度信号 `gpu_init_failed`，单独出现即可定论。正则里刻意**不含 `WGL` / `GLX`** ——
正则不区分大小写，而 `org.lwjgl` 里就含 `wgl`，那样每一条 LWJGL 堆栈都会被误判。
同理 `ccompat_error` 去掉了裸的 `shader`：Minecraft 核心类 `ShaderInstance` 会命中。

### 验证

- 新增 21 条测试（**270 项全过**），其中：
  - INFO/ERROR 成对对照，证明修的是"日志级别"这一个变量；
  - 两条负向对照专门钉住上面两个误报陷阱（`org.lwjgl`、`ShaderInstance`）；
  - 两条**静态守卫**：信号表里不许有"检测得到但没人用"的死信号，知识库里不许有
    "永远产不出来"的条目。它们各自带一条"提取非空"的前置断言，
    否则提取正则一旦失效，守卫会静默空转。
- 线上复测同一批日志：命中 **9/14 → 11/14**。剩下 3 项里，2 项是刻意设计的边界样例
  （一行碎片、一段与报错无关的抱怨，判 `need_log` 正确）；第 3 项是一份 12 行的
  崩溃报告，确实没有已知特征，现在归 `unknown`。

  这里有个刻意的取舍值得写下来：`unknown` 是"我没认出来"，`need_log` 是
  "你给的信息不够"。对一份完整的短报告，系统没有资格说后者。把两者混为一谈，
  就会逼着代码去编一个它并不掌握的结论。

---

## [1.11.3] — 补上 1.11.2 漏掉的"服务器"显示名

1.11.2 让纯客户端工单不再绑定服务器，但**没检查空 server_id 会流到哪些渲染点**。
在真实站点上跑端到端时发现三处：

```php
// src/Workflow.php:928、src/TicketMail.php:554、671（修复前）
$serverName = (string) (Config::server($serverId)['name'] ?? $serverId);
```

`server_id` 为空时 `Config::server('')` 返回 `null`，前半截取到 `null`，
`??` 接住的是后半截 —— 而后半截**也是空字符串**。于是玩家邮件里印出
「服务器：」、后面什么都没有，工单页上是一段空白。

### 做法

新增 `ticket_server_name()`（`src/helpers.php`），三处调用点统一改用它：

- `server_id` 为空（或纯空白）→ 返回「不涉及服务器（客户端问题）」
- 服务器能查到 → 返回配置里的名称，没写名称就回落到 id
- 服务器查不到（配置被删了）→ 回落到 id 本身

放在 helper 而不是各写一份，是因为这类"同一段取值逻辑抄三遍"正是漏掉其中
一处的成因。

### 一处自我更正

这次修复一开始被写成"顺带修掉了 PHP 警告"。**不对。** `??` 自带 `isset()`
语义，对 `null` 下标访问不产生任何诊断 —— 专门写的负向对照第一次运行就证明了
这一点，那条说法随即删掉。

所以这条改动是**纯显示修复 + 收敛重复逻辑**，跟错误日志无关。把它写成"修警告"
会让后来的人以为日志里本来有噪音，去找一个不存在的现象。

### 验证

- 新增 8 条单元测试（248 项全过）。其中两条是负向对照，钉死上面那个事实：
  旧写法产出空字符串、且**不**报 PHP 警告。
- 两条正则断言扫 `Workflow.php` / `TicketMail.php`，禁止
  `Config::server(...)[...]` 这种写法再出现。匹配前会先去掉 `//` 行注释 ——
  否则注释里举例说明这段历史写法时，断言会把自己扫挂（第一次跑就是这么挂的）。
- 线上站点实测：`?r=api&action=progress` 返回的 `server_name` 是占位文案，
  PHP 错误日志没有新增任何 `PHP Warning` / `Fatal` / `Notice`。

---

## [1.11.2] — 纯客户端报错不再要求先绑服务器

### 问题

玩家提交崩溃日志时，表单里的"服务器"是**必填**的。没有服务器的站点更糟：
整个表单被一张"请先配置服务器"的卡片顶替，玩家连字都打不进去。

可现实里一大半报错跟服务端没关系 —— 模组冲突、Java 版本不对、内存给少了、
某个 Mod 自身抛异常，这些全发生在玩家自己的电脑上。逼玩家先选一个服务器，
等于逼他随便选一个；那张工单于是带着一个假的服务端上下文进了诊断流程。

### 做法

**`server_id` 允许为空**，空字符串即"纯客户端工单"：

- `Workflow::submit()` —— 空值不再报错，`$server` 取 `[]`（不是 `null`，
  后续所有 `Config::server()` 查不到时的分支本来就走空数组）。
- `Workflow::verify()` —— 空 `server_id` 时**跳过 `Diagnosis::run()`**，
  直接构造一份合成诊断：`checks = []`、`suggestions = []`、
  `auto_fixable = false`、`issue = client_only`。拿一个空的 server_id
  去跑服务端诊断，只会得到一堆"读不到日志 / 连不上 RCON"的噪音。
- `ClientAdvisor::serverFixPlan()` —— **新增空 `server_id` 直接返回空计划**
  的守卫。这是唯一的收敛点：客户端分析照跑（模组冲突、Java 版本那些结论
  一条不少），但**任何服务端配方都不会被给出**。
- 表单 —— 必填去掉，加一个"不涉及服务器（纯客户端报错）"的选项，
  顶部提示改成不阻挡的说明条。

### 为什么守卫要加在 `serverFixPlan()`

客户端分析选配方时走的是 `Recipe::allowed($server, $recipe)`。服务器是空数组时，
"禁用名单"自然也是空的，于是**每个配方都算通过**，一路算下去就会给玩家建议
"重启服务端"。等真去执行时 `applyFix()` 才发现服务器不存在 —— 记一次失败，
白白吃掉这张工单的修复次数，还可能把熔断计数推上去。

所以守卫放在**产生建议**的那一步，而不是执行的那一步：没有服务器，
就不产生需要服务器的建议。

### 验证

- 单元测试：无服务器时 `server_fix.codes` 必须为空数组、`auto` 为 false、
  不给配方名；同一份日志配上服务器时结论照旧（反向对照，确认没误伤）。
- 端到端：零服务器站点上表单正常渲染（无阻挡卡片）、提交成功拿到工单号，
  诊断 `checks=0 / suggestions=0 / auto_fixable=0`、修复次数 0、`tasks` 表为空。

---

## [1.11.1] — 拆开 admin.php：路由归路由，动作归动作

纯位置调整，**行为零变化**。

### 问题

`public/controllers/admin.php` 原本 1341 行，里面混了三件事：

1. 入口、会话、CSRF、登录
2. 动作分发与页面路由
3. 十来个业务动作的实现（保存设置、增删服务器、改密码、通知渠道……）

第 3 类占了约 700 行，是"改一个功能就得在 1341 行里翻位置"的主要来源。

### 做法

把第 3 类的 **9 个函数整体搬到 `public/controllers/admin_actions.php`**：

```
admin_save_notify          admin_save_server        admin_change_password
admin_save_notify_channel  admin_delete_server      admin_run_maintenance
admin_add_server           admin_save_settings      admin_followup_pending_fixes
```

- **函数名和签名一个都没改**，调用点也不用动
- `admin.php` 只多了一行 `require`（在 use 块下面，任何动作分发之前）
- 1341 → **644 行**；新文件 733 行

### 搬之前先扫了依赖

把区域里用到的**短类名全部扫出来**，和 `use` 列表做差集 —— 确认 8 个类
（Cache / Config / ConsoleAuth / Db / Rate / Recipe / Task / Workflow）都已在列表里，
漏一个就是后台致命错误。这一步比事后调试便宜得多。

### 验证

237 项测试全过，外加一次**真实后台流程**：

- 7 个后台页面全部渲染正常
- 真的 POST 一次「保存设置」→ 302，且**配置真的写入**（证明搬走的 `admin_save_settings` 正常工作）
- **反向对照**：把那一行 `require` 注释掉，后台立刻坏 —— 既证明它是必需的，也证明这套验证确实在测东西

### 途中踩的两个坑（都记下来）

**① PowerShell 又把中文搞坏了。** 我用 `.ps1` 里的 here-string 拼新文件的注释，
而 PowerShell 5.1 把无 BOM 的 `.ps1` 当 ANSI 读 —— 多字节序列吃掉了一个换行符，
结果**两行注释并成一行，`require` 语句落进了 `//` 注释里**（等于永不加载）。
是我自己在这个会话里早就记录过的坑，又踩了一次。
改成脚本**只用 ASCII** 后正常。

**② 测试套件立刻抓到了搬家。** 有 6 条断言扫描 `admin.php` 找"默认 true 的开关"，
代码搬走后扫描落空 —— 它们报的是"扫到了关键开关：auto_fix（防止检查空转）"。
这正是那几条"防止检查空转"断言存在的意义。
修法是把扫描目标改成**两个文件都读**（测试的意图没变，只是代码分成了两个文件）。

---

## [1.11.0] — Multicraft 面板适配器（Apex Hosting 现在能接了）

起因是查主机商时发现的：**Apex Hosting 用的是 Multicraft**，而 Multicraft 是 Minecraft 世界里
最常见的面板之一，MCFix 现有四种适配器一个都对不上。用它的服主只能走 RCON
（**服务器挂了不能自动重启**）或用自定义接口自己抓包。

### 新增 — `src/Panel/MulticraftPanel.php`

| 能力 | 支持 |
|---|---|
| 下发游戏指令（白名单 / 解封 / 踢人 / 保存 / 重载） | ✅ |
| 开关机、**自动重启** | ✅ |
| 读服务端日志 | ✅ |
| CPU / 内存 / 玩家数 | ✅ |
| 列 mods 目录 / 读文件 / 取回模组 | ❌ **Multicraft 的 API 没有文件接口** |

能力矩阵**如实声明**，不假装支持文件操作 —— 后台点「测试面板 API」时会把边界一并告诉管理员。

后台新增一个「用户名」字段（`panel_username`）：Multicraft 是**唯一需要用户名 + 密钥两个凭据**的面板，
因为签名要用到用户名。没有复用一个名字对不上的现有字段（比如 `daemon_id`），
那种"字段名和实际含义不符"正是以后最难查的一类问题。

### ⚠️ 协议：网上流传的实现是错的

Multicraft 的认证不是把密钥原样发过去，而是算签名。我一开始参照一份 2013 年的单文件客户端，
它的算法是 `md5(apikey + method + user + 只有值)`。**跑到真源码（multicraft-py 的 `core/base.py`）才发现两处都不对：**

```
正确：message = 把每个参数的「键名 + 值」依次拼接（键名也要拼，无分隔符）
      签名    = hash_hmac('sha256', message, api_key)

旧实现：md5(apikey + method + user + 只有值)     ← 少了键名，且摘要算法不同
```

而且**最关键的动件名也和我猜的不一样**：不是 `sendCommand`，是 `sendConsoleCommand`，
参数还是 `server_id` 而不是 `id`。照猜的写，下发指令这个核心功能会直接是坏的。

这条已经写进适配器的注释和主机商索引 —— 照那份旧实现写会一直认证失败，
而面板只回一句含糊的错误，非常难查。

### 测试

222 → 237 项。除单元测试外，做了一次**端到端签名验证**：
写了一个**真的会校验签名**的假 Multicraft 面板（`mock-multicraft.php`），
签名不对就和真面板一样返回失败。结果：自检、下发指令、重启、读日志、读资源**全部通过**，
假面板一条签名错误都没记；再故意用错密钥跑一次，被正确拒绝 —— 证明那个 mock 确实在校验，
不是"怎么填都过"。

签名规格另用反射钉了三条单元测试（键名参与拼接 + HMAC-SHA256 / 不是那份过期实现 / 参数顺序有意义）。

---

## [1.10.5] — 文档与实现对齐；Pterodactyl 系主机商通用步骤

纯文档，无代码改动。

### 修正 — 安全文档还在写"全局计数"

1.10.4 把登录锁定的全局计数去掉了（那个计数能把管理员自己锁在后台外面），
但 `安全说明` / `SECURITY-NOTES` 里还写着"同时按 IP 和全局计数"。

已改成"**只按来源 IP 计数**；`admin.ip_allow` 白名单里的地址不受锁"，
并补了一段**防回归说明** —— 写清为什么不能用全局计数，
免得以后有人看到"按 IP 计数"觉得不够严、好心把它加回来。

会话指纹那行也顺手更正：改密码**或换后台路径**都会让其它会话失效，之前只写了改密码。

**这类问题的性质**：代码改了、文档还写着旧行为。审计当初就是靠"文档声称 X、实现是 Y"
抓出问题的 —— 补了代码不改文档，下次审计还会再报一遍。

### 新增 — Pterodactyl 系主机商的通用接入步骤

BisectHosting / PebbleHost / Sparked Host / GGServers 四家都是已查证的 Pterodactyl 系，
流程完全一样，所以不写四份重复教程，而是**共用步骤写一次**：

```
第 1 步  地址栏 /server/<短ID> → 服务器短 ID
第 2 步  /account/api → 建 ptlc_ 密钥（只显示一次）
第 3 步  后台逐字段对照表
第 4 步  ★ 执行器选 none —— 填错不报错，任务永远卡在"排队中"
第 5 步  点「测试面板 API」，看到 power = 自动重启可用
第 6 步  建议再配 RCON 兜底
```

第 4 步单独标了"最容易错"，因为选 `agent` 不会报任何错，
界面看起来正常，玩家只觉得"说修了没反应" —— 这是最难自查的一类问题。

---

## [1.10.4] — 登录锁定不再锁死管理员；玩家侧不再泄露服务端路径

1.10.1 审计里剩下的两条，外加文档与主机商索引的补齐。

### 修复 — 6 次错误登录会锁死**整个后台**（含管理员自己）

以前 `ConsoleAuth::gate()` 查的是**全局**计数：任何人错 6 次，整个后台就被锁上 ——
管理员自己也进不去，连登录表单都看不到。攻击者每 15 分钟重放 6 次就能**永久**把管理员关在门外。

这是拿可用性换安全，而且换错了方向：要防的是"某个来源正在爆破"，不是"全世界都在爆破"。

改成：

- **只按来源 IP 计数**（全局计数彻底移除）
- **`admin.ip_allow` 白名单里的地址不受锁** —— 管理员万一被自己的自动化脚本试错几次，
  还能从白名单入口进去

按 IP 锁已足够防爆破（换个 IP 要从头再数）。

**验证方式要说清楚**：这一条有 4 个源码级守卫（证明代码形状变了），
外加一次**真实 HTTP 登录流程**（错口令被拒、对口令 302、登录后能进后台）——
后者才是"没把登录改坏"的行为证据。**没有**做的是"模拟连续失败 6 次后管理员仍能进入"的端到端验证。

### 修复 — 玩家能看到服务端绝对路径

Agent 的诊断消息会带真实路径：

```
日志文件不存在或不可读：/home/steve/minecraft/logs/latest.log
```

而 `Workflow::playerChecks()` 把这个 message **原样**透给持有工单链接的玩家。
玩家改不了服务端文件，路径对他零价值，却暴露了服务器上的用户名和目录结构。

新增 `redact_paths()`：只留文件名（`…/latest.log`）。**管理员侧不受影响**，
走的另一条路仍是完整路径。

一条只能上真 PHP 才能确认的细节：`\w` 在 `/u` 修饰符下会匹配 Unicode 字母，
所以中文文件名会保留、中文目录照样抹掉 —— 效果正好是想要的。
本地用 Node 测不出来（JS 的 `\w` 只匹配 ASCII）。

### 文档 — 不再把"限流"说成"一次性"

`安全说明` / `SECURITY-NOTES` 里写着验证令牌是**一次性**的。实际是：
每枚令牌确实用过即废，但 `progress` 每次都会下发一枚**新的**可用令牌 ——
所以它不是"一人只能验证一次"，真正的闸门是**按来源 IP 的限流**（40 次/小时）。
令牌本身不可伪造（HMAC），所以这不是鉴权绕过，只是**不该把限流说成一次性**。

按用户决定**只改文档，不改行为**（改成真一次性会改变玩家端体验）。

### 文档 — 主机商接入索引（中英）

`deploy/主机商接入索引.md` + `HOSTS-INDEX.en.md`：一份"我用 XXX 主机商，能不能接"的对照表，
外加三分钟自己判断面板类型的清单。每一行都标来源（实测 / 官方文档 / 第三方文档 / 待查证）。

**BisectHosting 那条修正了一个错误结论**：我原先按第三方文档写成"基于 Pterodactyl"，
后来在开源 Multicraft 客户端库的主机商列表里发现它登记为 Multicraft
（`https://games.bisecthosting.com/api.php`）。两份可能都对（两种面板并存）。
已在《BisectHosting 接入指南》最前面加了**面板判别章节**，
并明确写出"如果是 Multicraft，MCFix 暂无适配器，只能走 RCON"。

**这正是"写错的教程比没有教程更糟"的实例** —— 用户会以为是自己在面板里填错了。

### 测试

211 → 222 项。另外做了一次**安全不变量复验**（17 项全过）：
从零安装、安装闸门、真实 HTTP 登录、敏感路径 404、开放重定向、CSRF（含安装第 3 步）。

---

## [1.10.3] — 主机商接入索引：我用 XXX 主机商，能不能接？

起因是一句反馈：**「我想 GitHub 中应该有你加入所有的面板的教程」**。

查下来发现，通用指南里**面板类型**那部分其实已经够扎实（MCSManager / 翼龙 / 宝塔 / 自定义
四类都有逐字段对照表），真正的缺口在**具体的主机商** —— 用户面对的是
「我在 Apex 租的服」，不是「我有一台 Pterodactyl」。

### 新增 — `deploy/主机商接入索引.md`（+ [EN](deploy/HOSTS-INDEX.en.md)）

一份「我用 XXX，能不能接」的对照表，外加**三分钟自己判断**的清单
（看地址栏 `/server/<id>` → 试 `/account/api` → 找 `server.properties` → 看有没有 SSH），
这样即使还没写到某家主机商，用户也能立刻知道该怎么配。

### 已查证的主机商

| 主机商 | 面板 | 能否直接接 |
|---|---|---|
| BisectHosting | Pterodactyl | ✅ |
| PebbleHost | Pterodactyl | ✅ |
| Sparked Host | Pterodactyl | ✅ |
| GGServers | Pterodactyl | ✅ |
| **Apex Hosting** | **Multicraft** | ❌ 无适配器 |
| Nitrado | 自研面板 + 自有 API | ❌ 需自定义接口 |
| Nodecraft / Shockbyte / GPORTAL | 未确认 | ❓ |

**结论：主流商业主机商里 Pterodactyl 系占多数**，所以翼龙适配器是这一块的主力，
BisectHosting 那份教程可以直接当模板。

### 发现 — Multicraft 是个真实缺口

**Apex Hosting 用的是 Multicraft**，而这是 Minecraft 世界里最常见的面板之一，
现有四种适配器一个都对不上。用它的服主只有 RCON（**服务器挂了不能自动重启**）
或用「自定义 HTTP 接口」自己抓包两条路。

好消息：Multicraft 有官方 API（`/api.php` 那套，带 `MulticraftAPI` 类），**写适配器是可行的**。
已列入待办。

### 文档可信度标注

这份索引里**每一行都标了来源**：

| 标记 | 含义 |
|---|---|
| ✅ 实测 | 有人真配通跑过 |
| 📄 第三方文档 / 官方文档 | 不是实测的，请自己确认 |
| ❓ 待查证 | 还没查，别照着猜 |

**为什么这么较真**：主机商换面板、关 API 页面都是常事，而**写错的教程比没有教程更糟** ——
用户会以为是自己填错了，来回折腾半天。所以没查的一律标"待查证"，
而不是凭"应该也是 Pterodactyl 吧"填上去。

---

## [1.10.2] — 一致性修复：别只在一个地方做对

1.10.1 那轮审计里，有一类问题性质相同：**某条规则在多数地方做对了，漏了一两处**。
单看漏掉的那处代码都不算错，但"声称的保护"和"实际的保护"对不上。
这一版把这四条补上，并加了 10 条**源码级一致性守卫**（这类修复没有可调用的行为入口，
只能盯住写法）。

### 修复 — 安装向导第 3 步没有 CSRF

第 3 步会往 `config/config.php` 里写服务器，**含 RCON 密码、Agent 令牌、自定义守护命令**，
是实打实的写操作。它以前只靠"已登录"这一条挡着。

而 `deploy/SECURITY-NOTES` 里写着"后台每个写操作都校验 CSRF" —— 这里就是那个例外，
文档和实现对不上。

修法：表单加 `csrfField()`，POST 加 `checkCsrf()`。
**第 2 步不能查** —— 那时还没有后台会话，它的闸门是"配置文件不存在"，有专门的用例盯着这个区别。

（跨站可利用性本来就低：后台 cookie 是 `SameSite=Lax`，跨站 POST 到不了已登录状态。
但"文档说不存在的东西"本身是个问题。）

### 修复 — 面板适配器的 test() 回显未脱敏的地址

`McsManagerPanel` 一直用 `mask_secret_url()`，但 **翼龙和宝塔没有**。
宝塔的 `api_url` 可能是 `/api/<32位密钥>/` 这种形状，直接回显等于把密钥
写进事件表的 `payload`（`bin/mcfix.php` 还会打到终端）。

### 修复 — stream 回退会跟随 302，把凭据重发到跳转目标

PHP 的 `http` wrapper 默认 `follow_location=1`，而 curl 分支的默认是**不跟随** ——
同一个方法的两条路行为不一致。跟随的后果是请求连同 `Authorization` 头一起被重发到跳转目标。

`PanelAdapter` 和 `AiAdvisor` 一直显式关着，`ChannelAdapter` 和 **Agent** 漏了。
通知渠道的 URL 是管理员填的、跳转到哪不可控；**Agent 那条更严重 —— 它的 body 里就是 Agent 令牌**。

⚠️ **改了 `agent/mcfix-agent.php`**：已经部署过 Agent 的服主需要重新上传覆盖那个文件。

### 修复 — config.example.php 把 trusted_proxies 的危险场景写反了

原文说"如果你的 Nginx 是**追加** X-Forwarded-For，请把 trusted_proxies 清空"。

**恰好说反了。** `client_ip()` 是**从右往左**扫 XFF 并跳过受信任代理的 ——
「追加」模式下代理会把真实对端写在最右边，代码拿到的就是它，**这是安全的**。

真正危险的是**原样透传**（`$http_x_forwarded_for`）或根本不设置这个头：
那时整条 XFF 都是客户端自己填的，攻击者每次换个 IP 就能绕过登录锁定和限流。
而原文对这种真正危险的写法只字未提。

已按实际行为重写，两种 Nginx 写法分别标注 ✅ / ⚠️。

（这次是先读了 `helpers.php:245-288` 确认扫描方向，再改的文档 —— 没直接采信审计的说法。）

### 新增 — README 顶部安全提示

说明 1.10.1 之前存在已修复的匿名邮件头注入漏洞，并提醒历史 tag `v1.9.0`
仍指向未修复的提交。

### 测试

201 → 211 项。新增的 10 条走反向对照验证：用**线上未修复版本**跑同一套测试，
新守卫全部报错 —— 确认它们不是摆设。

---

## [1.10.1] — 一轮完整安全审计后的修复

起因是一句要求：**「最后测试一遍，看有没有bug和严重的漏洞」**。

做了三件事：从交付包重跑全新装机演练、用假大模型跑端到端、以及**三个方向的并行代码审计**
（认证/会话/CSRF、注入/路径穿越、密钥泄露/SSRF/XSS）。审计结论我逐条自己复核过，
**其中一条被推翻**（见最后一节）。

测试总数 181 → 201，每条修复都配了**反向对照**：拿修复前的代码跑同一套新测试，
确认它真的会失败 —— 一条抓不到 bug 的回归测试等于没写。

### 修复 — 匿名可达的邮件头注入（最严重）

`Workflow::submit()` 里玩家提交的标题只做了 `trim()`，而 `trim()` **只去首尾**，
中间夹的 `CRLF` 原样保留。`Mail::encodeHeader()` 又只在含非 ASCII 字节时才 MIME 编码，
纯 ASCII 的换行直接进了 `Subject:` 头块。

于是一个**不需要登录**的 `POST ?r=api&action=submit`，标题填
`x\r\nReply-To: attacker@evil.tld` 就能往管理员收到的通知邮件里插任意头。

修法：在 `Mail::encodeHeader()` 这个汇聚点把 CR/LF/TAB/NUL 清成空格（所有头都受保护），
同时在 `Workflow::submit()` 源头也清一道。

**这一条的回归测试我写错了两版**，值得记下来：

- 第一版用了**含中文的标题** —— 旧代码对含非 ASCII 的标题会整体 MIME 编码，
  **顺手把 CRLF 也编码掉了**，所以旧代码照样"通过"。是反向对照发现它在旧代码上
  **没有失败**，才暴露出来。改用纯 ASCII 标题后才真正打中。
- 第二版断言写成"文本不存在"，但清理是**换成空格**，那些字仍在。
  正确断言是"不以行首形式存在"。

### 修复 — 玩家可控的 HTML 注入进管理员邮件

`TicketMail::renderHtml()` 里 `_..._` 那个分支匹配的是**未转义**的原始行，
`$m[1]` 被直接拼进 HTML 邮件。玩家在描述里写一行
`_<a href="钓鱼链接">…</a>_` 就能在管理员打开的邮件里渲染出任意 HTML。
旁边的 `MailChannel` 一直用的是转义后的值，这里漏了。

### 修复 — 真实崩溃报告的环境信息一条都解析不出来

`splitLines()` 不 trim，而 `parseCrashReport()` 的正则全是 `^` 锚定的。
**真实 crash-report 的 System Details 段全部是 Tab 缩进的**：

```
-- System Details --
Details:
	Minecraft Version: 1.20.1
	Java Version: 17.0.8, Microsoft
```

结果版本、Java、内存、CPU、系统**一个都提取不到**。影响玩家看到的体检结果、
`java_version` / `oom` / `version_mismatch` 的提示词，以及发给大模型的上下文。

修法：给这些"键: 值"模式加 `\s*` 前缀。**不能全局 trim** —— 下面判定堆栈行时
正是靠前导空白识别 `"\tat ..."`，trim 掉就认不出来了（这条也有专门的回归测试盯着）。

之前的测试用的都是"顶格写"的假日志，所以一直没暴露。

### 修复 — 空密钥/占位密钥签出的令牌可被伪造

`Token::hmac()` 在 `app.hmac_secret` 为空时会退回到字面量 `'mcfix-empty-secret'`。
而 `config.example.php` 里的 `'CHANGE_ME_TO_A_RANDOM_STRING'` **直接印在公开仓库里** ——
照抄示例不改，效果一样。用这种密钥签出来的令牌谁都能伪造，而 Agent 令牌
能让攻击者读走任务队列、伪造执行结果。

改成**失败关闭**：密钥为空或命中已知占位值时不签发任何有效签名，
拿不到令牌的管理员会当场发现配置有问题 —— 这比悄悄接受一个可伪造的令牌好。

### 修复 — 每服务器的配方禁用名单被绕过

后台可以按服务器逐个关掉修复动作（`servers[].recipes`）。
`Workflow::applyFix()` 正确检查了，但**玩家侧"取回 MOD"那条路直接调 `Executor::repair()`**，
只查了白名单和参数，漏掉了禁用名单 —— 管理员关掉的动作，玩家那边照样能排队。

修法：把检查下沉到 `Executor::repair()` 本身。它是公开入口，放这里以后新增调用方也不会再漏。

### 修复 — 换后台路径不踢会话，但界面说会

界面明确告诉管理员"换了路径之后这次会话不会带到新地址，需要重新输一次口令"，
代码注释也写着"旧路径上的会话 cookie 立刻失效"。

**两句都是假的。** `ConsoleAuth::scope()` 刻意返回站点根目录（注释写明隔离靠会话名），
而会话指纹只跟密码哈希有关 —— 换路径**踢不掉任何已存在的会话**。
管理员因为怀疑会话被盗而轮换路径时，攻击者的会话其实还活着。

修法：把后台路径混进 `sessionFingerprint()`，让那句承诺变成真的。

### 修复 — 管理员备注泄露给玩家

`progress()` 把工单的全部事件返回给**持有工单链接的玩家**，
而管理员备注（`feedback.note`）连同原文一起在里面。
`feedback.admin_note` 那一列本来就不对玩家暴露，说明备注的定位就是内部的 ——
事件这条路径漏了。

修法：`feedback.note` 不进玩家时间线；`feedback.reject` 只保留"被驳回"这个事实，
不带内部原话。

### 修复 — 后台「执行器」选项文案会把人劝退到错误选项

`none` 这一项界面上写的是「none（只诊断）」。但**面板服必须选它** ——
选了才会走 `panelFallback`，把面板 API 的能力用满（含自动重启）。
写成"只诊断"会让人去选 `agent`，而 `agent` 在商业面板服上**任务永远卡在队列里**，
界面不报错，玩家只觉得"说修了没反应"。

改成「none（面板服选这个：用面板 API 修）」。

### 新增 — BisectHosting 面板服接入教程

`deploy/BisectHosting接入指南.md`。查证到 **BisectHosting 的 Starbase 面板基于 Pterodactyl**，
所以现成的翼龙适配器可以直接用，两条路线（面板 API + RCON）逐字段对照了后台的真实文案。

### 一个被推翻的审计结论

审计报告里最"严重"的一条是：Agent 的 `run_recipe('restart_server')` 把
`$guard['restart_cmd']` 未转义地交给 `proc_open`（其余分支都用了 `escapeshellarg`），
并声称链路是"面板下发 → Agent 本地配置被更新 → 任意命令执行"。

**代码事实属实，但攻击链路不存在。** Agent 主循环里轮询响应**只读取 `tasks`**，
`guard` 字段从来没有被应用过；`execute_task` 读的是**本地** `config.php`。
而那两处"下发"（安装向导、后台服务器页）都只是**把配置渲染成文本给管理员复制**。

要改那个字段，你得已经能写 MC 机器的文件 —— 也就是已经有了代码执行权限。**不是漏洞。**

记录在这里是为了说明：审计结论不能直接采信，必须自己追一遍调用链。

### 仍未处理（需要产品决策，不是纯技术问题）

- 6 次错误登录会锁死**整个后台**（含管理员自己），每 15 分钟重放可永久锁死
- "一次性验证令牌"实际不是一次性的：`progress` 每次都会发一枚新的
- 玩家时间线会显示服务端日志的绝对路径
- 安装向导第 3 步没有 CSRF 令牌（`SameSite=Lax` 挡着跨站，但 `SECURITY-NOTES`
  声称"后台每个写操作都有 CSRF"是不准确的）
- `config.example.php` 关于 `trusted_proxies` 的说明把危险场景写反了
- 面板 `test()` 的 URL 没走脱敏，会进事件表 payload
- `ChannelAdapter` 的 stream 回退会跟随 302（带 Authorization 头），其余客户端都显式关了

---

## [1.10.0] — 服务器侧也能用大模型会诊（但它只能"挑"，不能"造"）

起因是一句反馈：**「我找不到任何填第三方 API 的地方。」**

查下来是个真 bug，而且症状很隐蔽 —— 见下面第一节。

### 修复 — 后台「大模型兜底」整段落在表单之外，填了也存不进去

`admin/settings.php` 里表单在第 277 行就 `</form>` 了，而「大模型兜底（可选）」
那一段（接口地址 / 模型名 / API 密钥）在第 279–333 行 —— **落在所有表单之外**。
页面上看得见、能填、能点保存，但浏览器提交时根本不带这些字段，
所以永远存不进去。后端 `admin_save_settings()` 读 `ai_base_url` 的代码一直是好的，
只是收不到数据。

**为什么之前的测试没发现**：测大模型功能时是直接用 `var_export` 往配置文件里写，
从没走过后台表单保存这条路。功能"能用"、界面"没用"，测试全绿。

修法是**删掉那个提前的 `</form>`**，让「自动化与风控 / 邮件 / 大模型」在同一个表单里提交 ——
不能拆成两个表单，因为 `admin_save_settings()` 是按"一次 POST 里的全部字段"
重建整个配置的，字段缺失就按默认值写回，单独提交大模型那段会把邮件设置清空。

同时补了回归测试（表单标签配对 + 没有输入框落在表单之外），
并做了**反向对照**：拿未修复的版本跑同一套测试，确认它确实会失败 ——
一条抓不到 bug 的回归测试等于没写。

### 新增 — 服务器侧大模型会诊

以前大模型只服务客户端报错。现在服务端诊断**一条问题都没分类出来**时，
也会问一次模型，让它在可执行动作里挑一个。

触发条件刻意收得很紧：只有规则库完全没命中才走这条路。
规则库给出了结论（哪怕只是"没验证到异常"）都不打扰模型 —— 既省钱，
也避免模型去推翻一套确定性规则。

### 安全边界 —— 模型是"选择题答题者"，不是"下命令的人"

| 环节 | 保证 |
|---|---|
| 递出去的清单 | 只含**当前这台服务器真能执行**、且参数来源明确的配方 |
| 模型返回的编号 | 必须先在清单里查到，**查不到就整条丢弃**，跟没问过一样 |
| 挑中了 | 还要再走一遍完整闸门：白名单 → 服务器是否允许 → 参数来源 → 审批判定 |
| 参数来源 | 只认 `player`（取自工单）和 `reason`（固定文案）。出现别的参数名直接放弃该动作 |
| 最终判定 | 复用 `Verdict::assemble()` —— 和规则库那条路是**同一段代码**，换不出更宽的权限 |

两条动作被**硬性排除**在模型可选项之外，因为挑错的后果不是"没修好"而是"修坏了"
或"开了不该开的口子"：

- `unban_player` —— 否则被封禁的玩家提交一张工单，就可能诱导模型把自己解封
- `clear_self_items` —— 误判等于直接毁掉玩家数据

这两条在规则库那条路上仍然可用（那是确定性的判断），只是模型碰不到。

### 测试

新增 20 项，其中一半是**攻击场景**，用假大模型真实跑通：

- 模型返回清单外的编号（`wipe_everything_and_rm_rf`）→ 整条丢弃，verdict 原样返回
- 模型返回禁止名单里的配方（`unban_player`）→ 拦下
- 模型返回参数来源不明的配方（`pull_mod`）→ 拦下
- 模型老实说判断不了 → 维持转人工
- 正常挑中一个动作 → 采纳，但重启类仍然 `requires_approval=1`（风控没被绕过）
- 大模型关闭时 → 完全不插手

测试总数 161 → 181。

---

## [1.9.0] — SMTP 发信（VPS 上唯一能真正发出去的通路）

起因是个很实在的追问：「要发送的话不是得填地址和密钥吗？不然怎么通过 QQ 官方的地址发送？」

**这个追问是对的，而且 1.7.2 的文档带着一个错的建议。** 在一台普通 VPS 上实测：

```
直连 mx3.qq.com:25          → 连不上
mx1.qq.com:25 / Gmail MX:25 → 都不通
smtp.qq.com:465 / :587      → 通
```

**服务商封了出网 25 端口**（VPS 的常规做法，防垃圾邮件）。而 PHP 的 `mail()`
干的事情正是"交给本机 MTA 直连对方 25 端口投递" —— 所以装了 Postfix 也没用，
只会把邮件堆在队列里，界面上看起来"装好了"，实际一封都发不出去。

### 新增 — 手写 SMTP 客户端（`src/Smtp.php`，零依赖）

不引入任何库，自己实现协议：

- 连接（465 隐式 SSL / 587 STARTTLS / 也可不加密）→ `EHLO` → 可选 `STARTTLS` +
  重新 `EHLO` → `AUTH LOGIN`（失败回退 `AUTH PLAIN`）→ `MAIL FROM` → `RCPT TO`
  → `DATA` → `QUIT`
- 服务器证书**校验证书**（自签会失败并明确报错，而不是默默降级——
  降级等于把授权码交给可能的中间人）
- 支持 SMTP 多行回复（`250-` 续行）
- 点号转义、统一 CRLF
- **认证信息不进日志**：`AUTH` 和那两行 base64 在 transcript 里被替换成"（已隐藏）"

### 错误提示写成"照着做就行"

这块花的时间不比协议本身少，因为 SMTP 的报错对普通用户基本是天书：

| 服务器返回 | 系统给出的提示 |
|---|---|
| `535` / `534` | 「**QQ 邮箱 / 163 邮箱这里要填"授权码"，不是登录密码** ——去邮箱网页版的「设置 → 账户」里开启 SMTP 服务并生成授权码（16 位字母）。另外：发件地址必须和登录账号完全一致。」+ 服务器原话 |
| `550` | 「收件人被拒绝：xxx」（并带上服务器原话） |
| 连不上 | 「连不上 host:port（原因）。检查：主机名/端口填对了吗？这家服务商封了这个端口吗？（QQ 邮箱用 smtp.qq.com + 465，不要用 25）」 |
| STARTTLS 失败 | 「服务器不支持 STARTTLS，可以把加密方式改成「SSL（465）」试试」 |
| `mail()` 失败 | 不再说"去装 Postfix"，改成：「VPS 上这条路基本走不通 —— 绝大多数服务商封了出网 25 端口，装了 Postfix 也只会把邮件堆在队列里。**请改用 SMTP**」 |

### 发件地址自动对齐

QQ / 163 要求 `MAIL FROM` 和登录账号完全一致，不一致会被 550 拒掉。
用户很难自己想到这一点，所以 `force_from_username` 默认开着，
填了别的发件地址也会自动改用登录账号，同时把正文里的 `From:` 头一起改掉
（不然头里写 A、信封写 B，一样过不了）。

### 后台

「系统设置 → 邮件通知」现在把 SMTP 放在最前面，并带一个折叠的对照表：

| 邮箱 | SMTP 服务器 | 端口 / 加密 | 密码填什么 |
|---|---|---|---|
| QQ 邮箱 | `smtp.qq.com` | 465 / SSL | **16 位授权码** |
| QQ 企业邮 | `smtp.exmail.qq.com` | 465 / SSL | 登录密码 |
| 163 / 126 | `smtp.163.com` | 465 / SSL | 授权码 |
| Gmail | `smtp.gmail.com` | 587 / STARTTLS | 应用专用密码 |
| 阿里云邮 | `smtp.mxhichina.com` | 465 / SSL | 登录密码 |

密钥/授权码**不回显到 HTML**（留空 = 不改，另有「清除已保存的授权码」）。
「测试邮件」按钮会把实际用的通路和服务器原话一起显示出来。

### 测试

冒烟测试 139 → **158 项**。另外写了个**裸 TCP 的假 SMTP 服务器**
（不是 HTTP），把它说的每句话都记下来，端到端验证了七节：

1. 协议对话顺序正确：`EHLO` → `AUTH LOGIN` → base64 账号/授权码 → `MAIL FROM` → `RCPT TO` → `DATA` → `QUIT`
2. 正文格式：`Date`/`From`/`To`/`Subject` 头齐全、中文主题 MIME 编码、`multipart/alternative` 两个分段、随机 boundary
3. 授权码填错 → 提示里确实指出「要填授权码」
4. 收件人被拒 → 提示里带上具体的地址和服务器原话
5. 发件地址被强制对齐到登录账号（配置写 `noreply@example.com`，实际发出的是登录账号）
6. 不配 SMTP 时回退 `mail()`，失败提示指向 SMTP
7. 日志里没有授权码

### 顺带

- `Mail::buildMessage()` 抽成公开方法，SMTP 和 `mail()` 共用同一份 MIME 组装，
  避免两条通路格式不一致。
- 新增的 PHP 7.4 兼容性扫描**当场抓到了我自己写的 `match`**（在 SMTP 的错误映射里）——
  已经改成 `switch`。

### 更正

1.7.2 的 CHANGELOG 和 README 里写的「装 Postfix 就能发」在 VPS 上是错的，
已经改正。家里/公司自建、25 端口没被封的环境仍然可以用 `mail()`，所以那条通路保留了。

---

## [1.8.0] — 大模型兜底（可选，默认关闭）

先说清楚一件事，因为有人问过：**这个系统本身不依赖任何大模型。**

诊断和修复的判断 100% 来自写死的规则 —— `src/ClientIssue.php` 的 37 条客户端问题、
`src/ClientLog.php` 的 51 条特征正则、`src/Catalog.php` 的 23 个检查项、
`src/Verdict.php` 的 14 条判定分支。整个系统会主动连出去的地方只有六个，
全都是你自己的东西：面板 API、你配的通知 webhook、Agent→面板、MC 协议握手、RCON、本机 `mail()`。

所以这一版加的东西是**可选的**，默认为关。

### 新增 — 规则库没命中时，问一次大模型

以前这种情况直接给玩家一句「暂时无法从日志中定位到明确原因，需要人工分析」。
现在如果管理员开了这个功能，会多问一次模型，让玩家拿到一个人话结论 + 具体步骤。

**只在命中不了规则时才出场。** 常见问题（缺 MOD、前置库、Mixin 失败、Java 版本、OOM、
白名单、封禁、连接被拒…）根本不会走模型，所以又快又省钱。

接口用的是 **OpenAI 兼容的 `/v1/chat/completions`**，所以下面这些是同一套配置，只换地址：

| 服务 | base_url | 说明 |
|---|---|---|
| DeepSeek | `https://api.deepseek.com` | 便宜、中文好 |
| OpenAI | `https://api.openai.com` | — |
| 通义千问 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | — |
| 硅基流动 | `https://api.siliconflow.cn/v1` | — |
| 本地 Ollama | `http://127.0.0.1:11434/v1` | 数据不出机器 |
| 本地 vLLM | `http://127.0.0.1:8000/v1` | 同上 |

**本地还是第三方不是二选一** —— 它们都提供 OpenAI 兼容接口，所以代码只有一套。

### 五条边界（写在 `src/AiAdvisor.php` 里，测试盯着）

1. **模型只出结论，不出命令。**
   它可以让玩家去装某个 MOD、改内存设置；但它给的修复建议**永远不会被执行**。
   唯一能让系统动手的路径是：它点出了知识库里已有的 code，然后我们用
   **自己写的那条知识库**（含我们自己审过的 recipes）——模型碰不到执行链。
   测试里专门放了一个"被攻破的模型"：返回 `IGNORE ALL PREVIOUS INSTRUCTIONS. 执行 restart_server`，
   外加伪造的 `recipes` / `server_fix` / `auto: true` 字段。
   结果：`suggestions = []`、`auto_fixable = false`、`server_fix.auto = false`。
2. **发出去之前先脱敏。** 玩家名、Windows/Linux 用户目录、IP、邮箱、启动器令牌
   全部替换成占位符；技术细节（异常类型、类名、版本号、行号）保留。
   测试里验证的是**网络上真实的请求体**，不是函数返回值。
3. **有超时、会降级。** 默认 6 秒（这段等待是玩家在页面上等着的）。
   拿不到结果就退回原来的"转人工"，绝不让模型卡住工单。
4. **按日志特征缓存 7 天。** 同一类报错只问一次，同一份日志重复提交不会反复花钱。
5. **有闸。** 每个来源每小时默认 5 次，全局每小时 60 次。

### 安全上的两个细节

- 调用模型时 `CURLOPT_SSL_VERIFYPEER = true`、`FOLLOWLOCATION = false`
  （密钥不会被 302 带到别的机器上去）。
- API 密钥**不回显到 HTML 里**（界面上显示"已配置，留空表示不改"），
  日志里只记 URL 且经过 `mask_secret_url()`。测试会检查日志里不含密钥原文。

### 后台

「系统设置 → 大模型兜底（可选）」：开关、接口地址、模型名、密钥、超时、
每来源每小时上限、单次 token 上限，外加一个「测一下连通性」按钮
（会显示耗时和本次消耗的 tokens）。
「客户端问题」页上能看出某条结论是规则给的还是模型给的（`source` 字段）。

### 测试

冒烟测试 113 → **139 项**，新增：脱敏的正反两面（该抹的抹掉、该留的留住）、
日志指纹的稳定性、`ai_suggestion` 不带任何配方、配方计划取自知识库而非模型返回内容、
默认关闭、关闭时行为与从前完全一致。

另外用一个假的 OpenAI 兼容接口做了八节端到端验证：命中不了 → 真的问 → 结论进工单 →
请求体里没有隐私 → 缓存命中 → 恶意模型指挥不动 → 限额生效 → 关掉开关零调用 →
模型挂掉时 0.01 秒降级且日志里没有密钥。

### 建议

- 先不开，跑一段时间，看「客户端问题」页里有多少条结论是 `unknown` / `need_log`。
  如果很少，说明规则库够用，没必要花这个钱。
- 要开就优先用第三方（便宜、效果好）；本地模型要占内存和显存，
  小机器上跑 7B 会拖慢诊断链路。

---

## [1.7.2] — 邮件通知（玩家回执 + 管理员提醒）

需求是「发了反馈自动填入收件人并发送，结束后自动删除」。做的时候又翻出三个
**一直存在但没人碰过**的后台 bug —— 它们都属于"界面看着好好的，实际点了没用"那一类。

### 新增 — 邮件模块（`src/TicketMail.php`）

和原来的「通知渠道」是两回事，分工写在后台界面上：

- **通知渠道**（`src/Notifier.php`）—— 钉钉/企微/飞书那套事件推送，给管理员和群看的
- **邮件模块** —— 给**玩家**发「已收到」和「处理结果」，顺带用邮件提醒管理员

配起来只要一个开关 + 一个管理员邮箱，不用建渠道、不用勾事件订阅。

- **玩家填写邮箱**（反馈页新增选填字段）：提交后自动发一封回执，修好或转人工时再发一封结果。
  上次填过的邮箱会记住（存在玩家自己的会话里），下次自动带上。
- **管理员邮箱**：每收到一条新反馈自动提醒，省得建渠道。
- **结束后自动删除**：工单解决 / 转人工 / 驳回 / 关闭时，把库里的玩家邮箱换成掩码
  （`ab***@qq.com`）。自由文本的「联系方式」里如果写了邮箱，也一并抹掉。

三条设计约束：

1. **发信不阻塞玩家**。提交接口只入队，真正的发送交给每分钟一次的 cron；
   失败按 1/3/10/30 分钟退避重试，最多 5 次。
2. **邮箱不长期留存**。队列里的明文地址在发出去（或彻底失败）后立刻清空，只留掩码展示。
3. **发信失败绝不影响主流程**。所有入口都是 try/catch + 记日志。

新增 `email_jobs` 表与 `feedback.player_email` / `email_purged_at` 两列，
老库升级时会自动 ALTER 补上（`Db::addColumnIfMissing()`）。

### 修复 — 后台开关「关不掉」（真实且影响安全）

浏览器**不会**提交没有勾选的复选框，而处理器写的是
`bool_param($input, 'x', true)` —— "字段缺失"被当成默认值 `true`，于是这些开关
**根本关不掉**，界面上的复选框形同虚设：

| 开关 | 后果 |
|---|---|
| 允许自动执行修复 | 关不掉。README 里承诺的"只验证不改动"做不到 |
| 重启服务端需要管理员批准 | 关不掉。文档教的"确认稳定后关掉它"做不到 |
| **逐条禁用修复配方（11 项）** | **一项都禁用不了** —— 这是安全边界，管理员取消勾选"清空玩家背包"实际仍然开着 |
| 启用该服务器 / 要求服务器在线 | 关不掉 |
| 启用面板 API | 关不掉 |

修法：每个复选框前面加一个同名的 `value="0"` 隐藏字段（隐藏字段先提交、复选框后提交，
PHP 取最后一个），勾选=1、不勾=0，行为终于和界面一致。

### 修复 — 停用的服务器从后台消失，再也点不回来

`admin/servers.php` 用的是 `Config::servers()`，而它会过滤掉 `enabled=false` 的服务器。
结果就是：取消勾选「启用该服务器」→ 那张卡片直接消失 → 没有任何入口能把它重新启用，
只能去改 `config.php`。

现在拆成两个方法：`Config::servers()`（过滤停用，玩家侧与诊断用）和
`Config::allServers()` / `Config::anyServer()`（后台管理用，不猜你的意图）。
后台卡片上会标「已停用（玩家看不到，也不会被诊断）」。

### 修复 — opcache 导致读到旧配置

`config/config.php` 是被 `require` 的 PHP 文件，所以也在 opcache 里。
opcache 默认 `revalidate_freq=2`（两秒才检查一次文件变化），于是"保存配置 → 下一个请求"
之间可能读到**上一版内容**。表现是「刚添加完服务器，点配置却说『服务器不存在』」——
排查时非常费解，因为 CLI 里读同一个文件完全正常。

现在 `Config::save()` 写完主动 `opcache_invalidate(..., true)`，
`Config::bootstrap()` 读之前也做一次 `opcache_invalidate(..., false)`（没改就几乎零开销）。

### 修复 — 一次局部保存会清空双域名配置

`admin_save_settings()` 里域名是这么写的：
`param($input, 'console_host', '')` —— 默认值给的是**空串**。
也就是说只要有一次 POST 没带上这两个字段（调接口、脚本局部提交、表单被改），
`domains` 就被清空，后台域名随即开始渲染玩家页面，管理员被关在门外。
现在改成"字段没提交就不覆盖"。

> 这个是我自己在写验证脚本时踩到的：脚本只 POST 了要改的几个字段，
> 结果把线上双域名配置冲掉了，后台域名立刻变成玩家页。已当场恢复，
> 但也正因为踩到了才把它修掉。

### 修复 — 后台通知按钮点了没反应（致命错误）

`public/controllers/admin.php` 用了 `Notifier::test()` / `retryPending()` / `dailyDigest()`，
但文件顶部没有 `use MCFix\Notifier;` —— 在无命名空间的文件里 `Notifier::`
会被解析成全局的 `\Notifier`，于是这三个按钮全是 "Class not found"。
语法检查完全看不出来，只有真的点下去才会炸。

### 测试

- 冒烟测试 85 → **113 项**，新增四类守卫：
  - **无命名空间的文件里不许有"忘了 use"的类引用**（上面那个通知按钮的 bug 就是它抓出来的）；
  - **PHP 8.0+ 专属语法扫描**（`match` / `?->` / `str_contains` / 枚举 / 只读属性 …）——
    README 承诺支持 7.4，而 CI 只在 8.3 上跑，这一列以前是空的；
  - **默认 true 的后台开关必须配一个 `value="0"` 隐藏字段**（防"关不掉"复发）；
  - 邮箱掩码、格式校验、邮件模块默认关闭。
- 一个"假 MTA"（把 `sendmail_path` 指向落盘脚本）跑通了完整链路：
  提交 → 入队 → cron 发送 → 结算 → 邮箱变掩码 → 失败重试与彻底失败后的明文清理。

### 已知限制

- 服务器上**没装 MTA** 的话 `mail()` 会直接失败（宝塔默认就没装）。
  系统会把原因写清楚：去软件商店装 Postfix，或配 `sendmail_path` 指向 msmtp。
- `mail()` 是"投递出去"而不是"对方收到"。QQ / 163 很容易判垃圾邮件，
  建议把发件人配成自己域名的地址并加 SPF 记录 —— 后台的测试邮件按钮也只能验证到投递这一层。

---

## [1.7.1] — 面板服通路修复 + 接入指南

起因：有人问「面板服怎么接」。为了写这份指南，我把面板 API 这条通路真正跑了一遍 ——
结果发现它**从来没成功过**，而且下面这三个问题 `php -l` 一个都查不出来。

### 修复 — 面板 API 整条通路

- **面板 API 完全不可用（致命错误）。** `src/Panel/PanelAdapter.php` 声明的是
  `namespace MCFix;`，而它躺在 `src/Panel/` 里 —— autoloader 按路径去找
  `MCFix\Panel\PanelAdapter`，加载到的文件却只定义了 `MCFix\PanelAdapter`，
  于是**四个面板适配器全部无法实例化**。只要在后台把面板类型从「不使用」改成任何一个，
  页面立刻 500。已改为 `namespace MCFix\Panel;`。
- **翼龙适配器还有第二、第三个致命错误。** `PterodactylPanel::endpoint()` 把父类的
  `protected` 方法收窄成 `private`；改成 protected 后又发现签名不兼容
  （父类 `endpoint(string $name, string $default)` vs 子类 `endpoint(string $suffix, array $query)`）。
  已把这个内部方法改名为 `serverUri()`，两件事分开，不再同名。
- **`MCSManagerPanel` 的接口和官方文档对不上。** 原来的写法是
  `POST /api/instance/command` + body `{"command":...}`、参数名 `remote_uuid`；
  而 MCSManager 现行文档是 `GET /api/protected_instance/command?command=...`、
  参数名 `daemonId`、开关机是 `/api/protected_instance/{open|stop|restart|kill}`、
  读文件是 `PUT /api/files/` + `{"target":...}`、控制台输出是
  `outputlog`（返回的 JSON 里再包一层 `data`）。
  现在**两代写法都会试**：先按现行文档发，失败退回旧版写法，
  并且同时带上 `daemonId` 与 `remote_uuid` 两个参数名。
- **MCSManager 缺必需的请求头。** 官方文档写明 `X-Requested-With: XMLHttpRequest`
  在没有另行指定时是必需的，而下发指令和开关机这两个动作都没带 ——
  也就是说即使路径对了，写操作也会被面板拒绝。

### 新增

- **`deploy/面板服接入指南.md`** —— 专门讲面板服：三档方案（只接面板 API / +RCON / +Agent）
  的能力对照、四个面板逐个的填写步骤、能力总表、排查手册，以及面板服特有的四个坑
  （地址填错被判成服务器离线、RCON 端口不通、面板密钥权限、控制台没有回显）。
- **`php bin/mcfix.php panel [server_id]`** —— 面板 API 体检：连通性、识别出的能力、
  实际发出的请求与面板的原始响应。填错 UUID 时这是最快的定位方式。
- **后台「接口路径覆盖」** —— 面板版本对不上时，可以在界面上按 `名称=路径`
  覆盖接口路径，不用改代码。配套文档教用 F12 抄面板自己的请求。
- **`mc_unreachable` 判定** —— 见下。

### 修复 — 判定

- **面板服最容易被误判的一种情况。** 系统判断"服务端在不在"的方式是从本机发一次
  Minecraft 协议握手。面板服如果把地址填错（填了节点内网地址、只配了 SRV 域名的地址、
  端口只对游戏流量放行），握手会失败，于是系统判「服务端离线」**critical** 并建议重启 ——
  对一台运行正常的机器。现在如果面板的资源接口（翼龙）或 MC 机器上的执行器
  确认进程在跑，会改判成 `mc_unreachable`（high，转人工、不建议重启），
  并在详情里说明更像是网络/地址问题。

### 修复 — 密钥泄漏

- **面板 API 密钥会明文进日志和操作日志。** MCSManager 把 `apikey` 放在 URL 查询串里，
  宝塔放 `request_token`/`request_sign`，Telegram 直接放在路径上 ——
  这些 URL 会被写进 `events` 表（后台「操作日志」页面）、`storage/logs`，
  以及 `bin/mcfix.php panel` 的输出。新增 `mask_secret_url()` 统一抹掉，
  面板适配器和通知渠道共用同一个函数（避免两边各写一份、其中一边漏掉某种形态）。

### 测试

- 冒烟测试从 43 项加到 **85 项**，其中三条是这次踩出来的教训：
  - **命名空间必须和目录一致**，并且要真的 `class_exists()` 加载一次
    —— 光看文本查不出来；
  - **子类不能收窄父类方法的可见性**（用反射逐个比对）；
  - **密钥掩码**必须真的把密钥抹掉、同时留下可辨认的前缀。
- 另外用两个假面板（严格按官方文档校验请求头、参数名、响应结构）
  把「面板服」这条通路端到端跑通：连通性 → 诊断读日志 → 面板控制台下发指令 →
  能力不足时的报错是否人话。

### 已知限制（这轮之后仍然存在）

- 面板适配器只在**假的**面板上验证过请求/响应的形状，没有在真实的 MCSManager /
  翼龙实例上端到端跑过。路径对不上时请用「接口路径覆盖」自救，并欢迎反馈。
- MCSManager 的 API 不暴露进程/磁盘/CPU，所以「只有面板 API」时这几项采不到。

---

## [1.7.0] — 首次公开前的安全与可用性复查

发布前请了两位独立审阅者做了一轮通读（安全 + 工程质量），下面是这轮修掉的东西。
标 **[安全]** 的是复查中发现的真实问题，不是假设。

### 修复 — 安全

- **[安全] 安装向导被当成提权通道。** 原判定是"`admin_password` 为空就允许安装"，
  于是只要口令字段被清空（无论是管理员照着旧提示手动删的还是配置写坏），
  **任何未认证的公网请求** POST 一次 `?r=install&step=2` 就能自己设一个口令，
  并当场拿到一个已登录的后台会话 —— 完整的接管链路。
  现在改为：**只要 `config/config.php` 存在，安装向导就永久关闭写口令的分支**；
  忘记口令只能上服务器执行 `php bin/mcfix.php reset-password`（新增命令）。
  登录页与 `ConsoleAuth::login()` 的提示也一并改了。
- **[安全] 所有出网请求都关掉了 TLS 证书校验**（面板 API、通知渠道、Agent）。
  面板密钥、下发的游戏指令、读回的日志都走这些连接，等于全程可被中间人改写。
  现在默认**校验**；只有管理员在服务器配置里显式勾选"跳过 HTTPS 证书校验"才关闭，
  界面上写明了代价。面板适配器同时去掉了 `FOLLOWLOCATION`（带密钥的请求不该被 302 带走）。
- **[安全] 转发头被无条件信任，IP 白名单与限流可被伪造。**
  旧实现只要 `REMOTE_ADDR` 是内网就采信 `X-Forwarded-For` 的第一段 ——
  而宝塔 Nginx 默认是**追加**，第一段正是客户端能自己写的。
  现在只有 `trusted_proxies` 里列出的来源才会采信，并且从**最右侧**取第一个非代理地址。
- **[安全] 后台表单的 `redirect` 参数可被用来做开放重定向。**
  现在只接受站内相对路径或与本后台同前缀的绝对地址。
- **[安全] Agent 令牌输入框会毁掉令牌。** 旧界面把令牌截断成前 8 位显示，
  保存任何其它设置时都会原样写回，把一个真令牌换成 `abcd1234…`。
  现在只读展示，另设一个"换成新令牌"输入框（填 `__new__` 重新生成）。
- **[安全] `/health` 向匿名访问者泄露站点自检结论**（缺哪个扩展、几台服务器等）。
  现在匿名只返回"活着 + 版本号"，详细信息仅限本机来源。
- **[安全] Telegram 机器人令牌会明文进日志。** 它在 URL 路径上而不是查询串里，
  原来的掩码函数只处理查询串 —— 令牌就这么进了 `notifications.last_error` 和操作日志。
- **[安全] 客户端日志的正则放大面收窄**：单行截断 4000 → 600 字符，
  并新增总行数上限 6000 行（原来 256 KB 短行能有十几万行，每行都要过几十条正则）。
- **[安全] MOD 列表接口返回服务器绝对路径**，且绕过了后台 IP 白名单。该接口本身
  在路由上永远拿不到登录态（是死代码），已整体移除。

### 修复 — 可用性（照文档做会踩到的）

- **安装向导第 3 步走不到。** 完成第 2 步后配置里已经有口令了，旧判定立刻把向导关掉，
  于是"玩家反馈链接 / Agent 配置 / systemd 命令"这一整屏**永远显示不出来**。
  现在第 2 步用后台自己的会话，第 3 步按"已登录"放行。
- **生成的 Agent 配置缺 `rcon` 段。** 结果就是 Agent 能连上、却一个配方都执行不了，
  而且报错很难定位。现在 `Agent::bootstrapConfig()` 一次性下发全部字段
  （含 `rcon`、`log_path`、`listen_port`、`disk_path`、自定义守护命令），
  安装向导与后台服务器页都直接用它，不再各抄一份。
- **`unmute_player` 结构上不可能成功**：它声明走 Agent，但面板侧 RCON 没有对应指令模板。
  现在补上了 `unmute <玩家>` 模板并改为 `panel` 优先，Agent 侧仍是兜底。
- **文档里的计划任务命令是错的**：网页里 `PHP_BINARY` 的值在宝塔下是 `php-fpm`，
  贴进计划任务只会起一个 FPM 进程。新增 `cli_php_path()` 统一推导
  `bin/php`，并在界面上提醒先 `which php`。
- **`config.example.php` 照抄会得到一个进不去的后台**：口令为空、`admin.path` 为空、
  密钥是占位符，而后台路径是按密钥派生的、文件里又没写。现在文件头直接说明
  "这是字段说明，请走安装向导"，`admin.path` 给出必须显式填写的占位值，
  并补齐 `db.*`、`agent_ip_allow`、`panel.insecure`、`queue_ttl`、`dispatch_wait`
  以及 `MCFIX_*` 环境变量。
- **`deploy/check-close-tag.js` 里写死了作者的 Windows 路径**，别人跑必然 ENOENT。已改为相对本仓库。
- **`agent_ip_allow` 只在文档里存在**，配置样例和后台表单都没有，等于没法配。现在两处都有。
- **通知在渠道不可用时静默消失。** 现在会写一条 `notify.skip` 操作日志，并说明原因
  （渠道已关闭 / 类型未知 / 缺少哪个必填项）。
- **玩家接口没有异常兜底**：任何未捕获的错误都会变成"HTTP 200 + 空 body"，
  玩家只看到"提交失败"，服务端也没有线索。现在统一返回 500 JSON 并记日志。
- **管理员手动"标记已解决"会被显示成"系统自动处理"。** `auto_fixed` 现在只有
  在系统真的下发过修复时才置位。
- **后台页面的 `bodyClass` 从来没生效**（`render_layout()` 没有透传），宽栏样式一直没应用。
- **玩家侧会话是 PHP 隐式创建的默认会话**（没有 HttpOnly / SameSite）。
  现在显式用 `mcfix_start_session('mcfix_sid', ...)`。
- 后台维护工具里那个 `Cache::forget('ping:survival')` 是作者自己服务器的硬编码 ID，
  现在改为遍历所有服务器清探测缓存，并在界面上补上了对应按钮。
- 文档里的"看到 `心跳成功` 就说明通了"是错的（Agent 从来不打这行字）、
  "反馈链接 24 小时有效"对分享链接不成立（30 天）、
  备份示例用的 `cp` 在 WAL 模式下可能拿到不一致快照（改用 `sqlite3 .backup`）。

### 变更

- 移除死代码：`csrf_token()` / `csrf_field()` / `csrf_check()`（玩家表单里那个隐藏
  `csrf` 字段从来没有被校验过，是安全剧场）、`?r=mod.list`、
  `Cache::remember()` / `Cache::bump()`、`Net::readLine()`、
  `ServerMods::modInfo()` / `fileNames()`、`ServerLog::modsFromLog()`。
- `PanelAdapter` 新增 `resources()` 默认实现，去掉调用方的 `method_exists` 探测。
- README 增加「现状与已知限制」与「界面长什么样」两节，并补齐 Nginx deny 规则示例
  （`.htaccess` 对 Nginx 无效）。
- 新增 `.github/`（Issue / PR 模板 + PHP 7.4 & 8.3 的 `php -l` CI）与 `tests/run.php` 冒烟测试。

---

## [1.6.4] — 打包日（见上方日期说明）

首次公开前的完整实战部署，修掉了 8 个只有真实环境才会暴露的问题。

### 修复

- **后台无法登录**：cookie 的 `path` 被设成后台私有路径，而真实请求路径是
  `/index.php`，两者不匹配导致浏览器不回传 cookie，登录状态永远保持不住。
  现在 cookie 作用域跟随站点基准目录，会话隔离改由会话名（`mcfix_console`
  与 `mcfix_sid`）和独立域名共同保证。
- **分享链接与工单链接失效**：`baseUrlForHost()` 已补 `/`，调用方又传了前导
  `?`，拼出 `https://域名/??r=feedback&t=...`。现在统一由底层补 `?`。
- **`?r=api&action=xxx` 写法不可用**：路由只从 `?r=` 拆控制器，没有回退读取
  `action` 参数，导致返回「未知接口」。现在支持点号与 `&action=` 两种写法。
- **CLI / cron 环境把 HTTPS 站点生成成 HTTP 链接**：协议判定改为优先读
  `app.base_url`（CLI 下没有 `$_SERVER`）。
- **后台 500**：`render_layout()` 定义在 `public/index.php` 中段，而后台入口在
  该文件早于函数定义处就被 `require`。函数已移入 `src/helpers.php`。
- **语法错误**：`src/ClientLog.php` 的正则 `white-?listed` 中 `?l` 紧邻，
  PHP 会把它当作 `?>` 结束标签，字符串被截断。已改写为 `white(?:-)?listed`，
  并新增 `deploy/check-close-tag.js` 扫描同类问题。
- **`admin/help.php` 语法错误**：`<pre>` 里的 `<?php` 是真正的开标签，内容被当
  代码执行。已改为用 `e()` 输出转义后的示例文本。
- **`setup-domains.sh` 无限递归**：Let's Encrypt 把两个域名放在同一个证书目录，
  而脚本按第二个域名去查证书，判断为「无证书」后重复安装 certbot。
  已改为按主域名目录判断。

### 变更

- **后台改为独立域名**（可选但推荐）：原先只支持「同域名 + 私有路径」，
  现在支持 `domains.console_host` 独立域名，同源策略天然隔离。
  留空则退回私有路径模式，行为不变。
- 引入执行器「能力声明」：面板适配器声明自己能做什么（`console` / `power` /
  `read_log` / `list_dir` / `read_file` / `resources`），上层按能力降级而非假设全能。
- `src/ServerLog.php`：服务端日志分析从 Agent 抽出，面板 API 与 Agent 共用同一套
  判定，避免同一报错在两种通道下结论不一致。

### 新增

- `deploy/check-close-tag.js`：扫描字符串内的 `?>`（真实语法错误的来源）。
- `deploy/check-alt-syntax.js`：替代语法配对检查。
- `deploy/install.sh`：一键创建运行时目录并修权限。

---

## [1.6.0] — 打包日（见上方日期说明）

### 变更

- **后台与玩家反馈界面彻底分离**：
  - 后台入口从 `?r=admin` 改为随机私有路径（`console-<16位随机>`），
    安装向导生成；`?r=admin` 返回不带任何信息的 404。
  - 独立会话名 `mcfix_console`（玩家侧为 `mcfix_sid`），互不读取。
  - 后台独立的 CSRF 令牌（`console_csrf`）。
  - 玩家页面移除全部指向后台的链接；未登录时后台只显示「访问验证」。
  - 新增 IP 白名单（`admin.ip_allow`，支持单 IP / 通配 / CIDR）。
  - 登录失败 15 分钟 6 次锁定，按 IP 与全局双计数。

### 修复

- 通知里的后台链接、`bin/mcfix.php` 的掉线告警链接仍指向旧的 `?r=admin`。

---

## [1.5.0] — 打包日（见上方日期说明）

### 新增

- **通知引擎**（`src/Notifier.php`）：事件分发、去重、风暴保护、失败重试队列。
- **10 个通知渠道**（`src/Channel/`）：钉钉、企业微信、飞书、邮件、Bark、ntfy、
  Server 酱、Telegram、Discord、通用 Webhook。
- 后台「通知设置」页：渠道配置、事件订阅勾选表、发送记录与手动重试。
- 计划任务新增：通知重试、每日汇总、Agent 掉线告警。

---

## [1.4.0] — 打包日（见上方日期说明）

### 新增

- **面板 API 执行器**（`src/Panel/`）：MCSManager、翼龙 / Pterodactyl、宝塔面板、
  自定义 HTTP 接口。
- **能力声明与自动降级**（`src/Executor.php`）：诊断读日志优先面板 API，
  游戏指令优先 RCON，进程类动作优先 Agent。
- `src/ServerLog.php` 服务端日志分析。
- 宝塔接口签名实现（`request_token` + `request_sign`）。

---

## [1.3.0] — 打包日（见上方日期说明）

### 新增

- **客户端崩溃日志解析**（`src/ClientLog.php`）：支持崩溃报告、`latest.log`、
  启动器日志；提取版本、加载器、Java、内存、MOD 列表、异常栈、缺失的类。
- **客户端问题知识库**（`src/ClientIssue.php`）：40+ 条错误特征与对应解决步骤。
- **服务端 MOD 交叉比对**（`src/ServerMods.php`）：判断玩家多装 / 少装了什么。
- **MOD 自动分发**（`src/ModLibrary.php`）：Agent 从服务端 `mods/` 目录取回文件，
  玩家直接下载，版本一定匹配。
- 后台「客户端问题」页：按问题码聚合、MOD 库管理。

---

## [1.2.0] — 打包日（见上方日期说明）

首个可用版本。

### 新增

- 玩家侧：签名链接反馈、页内自动验证、实时进度、工单链接。
- 诊断引擎（`src/Diagnosis.php`）：Minecraft 协议握手、RCON 查询、日志分析、
  进程 / 端口 / 磁盘采集。
- 判定器（`src/Verdict.php`）：检查结果 → 问题码 + 严重级别 + 修复建议。
- 修复配方白名单（`src/Recipe.php`）与执行器路由（`src/Executor.php`）。
- MC 侧 Agent（`agent/mcfix-agent.php`）：长轮询领任务、执行白名单配方。
- 业务编排（`src/Workflow.php`）：提交 → 验证 → 修复 → 复验 → 转人工。
- 风控：修复次数上限、重启小时配额、连续失败熔断、IP 限流。
- 管理后台：工单、服务器与 Agent、操作日志、系统设置、接入文档。
- 命令行工具（`bin/mcfix.php`）。
- 宝塔部署文档与一键安装脚本。
